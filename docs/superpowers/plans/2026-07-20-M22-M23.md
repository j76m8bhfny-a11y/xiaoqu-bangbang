# M22 宠物帮帮 + M23 购物拼拼 实现计划

**目标：** 在邻里帮 tab 下新增"宠物帮帮"（代喂/代遛/寻宠）和"购物拼拼"（求代购/代购方）两个互助子模块，复用互助瀑布流和 MasonryEventCard 卡片。

**架构：** M22 复用 events 表加 subType+petMeta 两个字段；M23 新建 group_buys+group_buy_items 两张独立表。两个模块分 2 个批次实现，每批次完成后跑 /review。

## 全局约束

- **Ponytail 懒汉模式**：最少代码、用标准库/已有依赖、不加未请求的抽象
- **TDD**：先写失败测试，再写代码让它通过（RED -> GREEN -> REFACTOR）
- **工作流**：每个批次完成后跑 /review
- **测试 DB**：gengwang_db 端口 5433
- **后端测试命令**：`cd apps/api && npm test`
- **前端构建命令**：`cd apps/miniapp && npm run build:weapp`
- **Admin 构建**：`cd apps/admin && npm run build`
- **契约红线**：`packages/shared/src/api.ts` 是前后端共享 DTO 唯一定义点
- **小区数据隔离**：所有 Service 方法必须接收 communityId 并在 Prisma where 中过滤

---

# 批次 1：M22 宠物帮帮

## Task 1: Prisma schema 加 subType + petMeta 字段

**文件：**

- 修改: `apps/api/prisma/schema.prisma` (model Event, 约 241 行后)
- 创建: `apps/api/prisma/migrations/20260720000001_add_pet_help_sub_type/migration.sql`

- [ ] **Step 1: 修改 schema.prisma model Event**

在 `type String` 行后加 2 行：

```prisma
  type             String // help_request/public_welfare/pet_help/lost_found/public_feedback/discussion
                          // (help_offer/lost_found @deprecated, 仅兼容历史数据)
  subType          String?   // pet_help 子分类: 'feed'|'walk'|'lost' (其他类型为 null)
  petMeta          Json?     // pet_help 专属字段 JSON (feed/walk/lost 各自字段)
```

在 `@@index([communityId, type, status, createdAt])` 行后加：

```prisma
  @@index([communityId, type, subType, status, createdAt])
```

- [ ] **Step 2: 创建 migration SQL**

`apps/api/prisma/migrations/20260720000001_add_pet_help_sub_type/migration.sql`:

```sql
ALTER TABLE "events" ADD COLUMN "sub_type" TEXT;
ALTER TABLE "events" ADD COLUMN "pet_meta" JSONB;

-- 老数据迁移：lost_found -> pet_help + subType=lost + petMeta
UPDATE "events"
SET "type" = 'pet_help',
    "sub_type" = 'lost',
    "pet_meta" = jsonb_build_object(
      'petType', NULL,
      'breed', NULL,
      'name', "title",
      'lostLocation', "location_text",
      'lostTime', "event_time",
      'appearance', "description",
      'photos', "images"
    )
WHERE "type" = 'lost_found';

CREATE INDEX "events_community_type_subtype_status_created_idx"
  ON "events" ("community_id", "type", "sub_type", "status", "created_at");
```

- [ ] **Step 3: 执行迁移**

```bash
cd apps/api && npx prisma migrate dev --name add_pet_help_sub_type
```

- [ ] **Step 4: 生成 Prisma Client**

```bash
pnpm db:generate
```

- [ ] **Step 5: 验证迁移成功**

```bash
# 检查 events 表新列
psql -h localhost -p 5433 -U postgres -d gengwang_db -c "\d events" | grep -E "sub_type|pet_meta"
# 检查老数据迁移（应为 0）
psql -h localhost -p 5433 -U postgres -d gengwang_db -c "SELECT count(*) FROM events WHERE type='lost_found';"
```

- [ ] **Step 6: 提交**

```bash
git add apps/api/prisma/schema.prisma apps/api/prisma/migrations/20260720000001_add_pet_help_sub_type/
git commit -m "feat(M22): add subType + petMeta columns to events table, migrate lost_found data"
```

---

## Task 2: 共享契约加 PET_HELP + PetSubType 枚举

**文件：**

- 修改: `packages/shared/src/enums.ts` (EventType 枚举附近)
- 修改: `packages/shared/src/api.ts` (CreateEventRequest + EventDto 附近)

- [ ] **Step 1: enums.ts 加 PET_HELP + PetSubType**

在 `EventType` 枚举内加：

```ts
export enum EventType {
  HELP_REQUEST = 'help_request',
  /** @deprecated help_offer 已废弃，前端不再允许创建，仅兼容历史数据 */
  HELP_OFFER = 'help_offer',
  PUBLIC_WELFARE = 'public_welfare',
  PET_HELP = 'pet_help',
  /** @deprecated lost_found 已迁移到 pet_help + subType=lost */
  LOST_FOUND = 'lost_found',
  PUBLIC_FEEDBACK = 'public_feedback',
  DISCUSSION = 'discussion',
}

/** 宠物帮帮子分类 */
export enum PetSubType {
  FEED = 'feed',
  WALK = 'walk',
  LOST = 'lost',
}
```

- [ ] **Step 2: api.ts 加 PetMeta 接口 + 扩展 CreateEventRequest/EventDto**

在 `CreateEventRequest` 之前加：

```ts
/** 代喂专属字段 */
export interface PetFeedMeta {
  petType: 'cat' | 'dog' | 'fish' | 'other';
  petName?: string;
  feedsPerDay: number;
  totalDays: number;
  dateRange: { start: string; end: string };
  needClean: boolean;
  rewardType: RewardType;
  note?: string;
}

/** 代遛专属字段 */
export interface PetWalkMeta {
  dogSize: 'small' | 'medium' | 'large';
  dogName?: string;
  timesPerDay: number;
  durationPerTime: number;
  timeSlots: Array<'morning' | 'noon' | 'evening' | 'night'>;
  needGear: boolean;
  rewardType: RewardType;
  note?: string;
}

/** 寻宠专属字段 */
export interface PetLostMeta {
  petType: string;
  breed?: string;
  name?: string;
  lostLocation: string;
  lostTime: string;
  appearance?: string;
  photos?: string[];
  rewardType: RewardType;
  note?: string;
}

export type PetMeta = PetFeedMeta | PetWalkMeta | PetLostMeta;
```

`CreateEventRequest` 末尾加 2 个可选字段：

```ts
export interface CreateEventRequest {
  // ... 现有字段 ...
  subType?: PetSubType;
  petMeta?: PetMeta;
}
```

`EventDto` 末尾加 2 个可选字段：

```ts
export interface EventDto {
  // ... 现有字段 ...
  subType?: PetSubType | null;
  petMeta?: PetMeta | null;
}
```

- [ ] **Step 3: 构建共享包**

```bash
cd packages/shared && pnpm build
```

- [ ] **Step 4: 提交**

```bash
git add packages/shared/src/enums.ts packages/shared/src/api.ts packages/shared/dist/
git commit -m "feat(M22): add PET_HELP EventType + PetSubType enum + PetMeta interfaces"
```

---

## Task 3: 写失败测试 - M22 后端创建 + 认证前置 + subType 校验

**文件：**

- 创建: `apps/api/test/event-pet-help.spec.ts`

- [ ] **Step 1: 写测试文件**

参考 `apps/api/test/events.spec.ts` 的 setup 模式，写以下用例：

```ts
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import { Test, TestingModule } from '@nestjs/testing';
import { INestApplication, ValidationPipe } from '@nestjs/common';
import request from 'supertest';
import { AppModule } from '../src/app.module';
import { PrismaService } from '../src/prisma/prisma.service';

describe('Feature: 宠物帮帮 (M22)', () => {
  let app: INestApplication;
  let prisma: PrismaService;
  let verifiedToken: string;
  let verifiedUserId: string;
  let unverifiedToken: string;
  let unverifiedUserId: string;
  let communityId: string;

  beforeAll(async () => {
    const moduleFixture: TestingModule = await Test.createTestingModule({
      imports: [AppModule],
    }).compile();
    app = moduleFixture.createNestApplication();
    app.setGlobalPrefix('api/v1');
    app.useGlobalPipes(
      new ValidationPipe({ whitelist: true, forbidNonWhitelisted: true, transform: true }),
    );
    await app.init();
    prisma = app.get(PrismaService);

    const community = await prisma.community.create({
      data: { name: '宠物帮测试小区', city: '南京', district: '鼓楼区', address: '宠物路1号' },
    });
    communityId = community.id;

    // 创建已认证用户
    const verifiedRes = await request(app.getHttpServer())
      .post('/api/v1/auth/wechat-login')
      .send({ code: 'pet-help-verified-user' });
    verifiedToken = verifiedRes.body.data.token;
    verifiedUserId = verifiedRes.body.data.user.id;
    await request(app.getHttpServer())
      .post('/api/v1/communities/select')
      .set('Authorization', `Bearer ${verifiedToken}`)
      .send({ communityId });
    // 直接更新 verifyStatus 为 verified
    await prisma.user.update({
      where: { id: verifiedUserId },
      data: { verifyStatus: 'verified' },
    });

    // 创建未认证用户
    const unverifiedRes = await request(app.getHttpServer())
      .post('/api/v1/auth/wechat-login')
      .send({ code: 'pet-help-unverified-user' });
    unverifiedToken = unverifiedRes.body.data.token;
    unverifiedUserId = unverifiedRes.body.data.user.id;
    await request(app.getHttpServer())
      .post('/api/v1/communities/select')
      .set('Authorization', `Bearer ${unverifiedToken}`)
      .send({ communityId });
  });

  afterAll(async () => {
    await prisma.event.deleteMany({ where: { communityId } });
    await prisma.communityMember.deleteMany({ where: { communityId } });
    await prisma.community.delete({ where: { id: communityId } });
    await prisma.user.deleteMany({
      where: { id: { in: [verifiedUserId, unverifiedUserId] } },
    });
    await app.close();
  });

  describe('创建 pet_help 事件', () => {
    it('已认证用户创建 feed 成功', async () => {
      const res = await request(app.getHttpServer())
        .post('/api/v1/events')
        .set('Authorization', `Bearer ${verifiedToken}`)
        .send({
          type: 'pet_help',
          subType: 'feed',
          title: '代喂猫咪',
          description: '出差 3 天，求邻居帮忙喂猫',
          petMeta: {
            petType: 'cat',
            petName: '小白',
            feedsPerDay: 2,
            totalDays: 3,
            dateRange: { start: '2026-08-01', end: '2026-08-03' },
            needClean: true,
            rewardType: 'free',
            note: '猫粮在玄关柜',
          },
        });
      expect(res.status).toBe(201);
      expect(res.body.data.type).toBe('pet_help');
      expect(res.body.data.subType).toBe('feed');
      expect(res.body.data.petMeta.petType).toBe('cat');
    });

    it('已认证用户创建 walk 成功', async () => {
      const res = await request(app.getHttpServer())
        .post('/api/v1/events')
        .set('Authorization', `Bearer ${verifiedToken}`)
        .send({
          type: 'pet_help',
          subType: 'walk',
          title: '代遛金毛',
          description: '出差 3 天，求邻居帮忙遛狗',
          petMeta: {
            dogSize: 'large',
            dogName: '大毛',
            timesPerDay: 2,
            durationPerTime: 30,
            timeSlots: ['morning', 'evening'],
            needGear: true,
            rewardType: 'negotiable',
            note: '牵引绳在门口',
          },
        });
      expect(res.status).toBe(201);
      expect(res.body.data.subType).toBe('walk');
    });

    it('未认证用户创建 lost 成功（不需要认证）', async () => {
      const res = await request(app.getHttpServer())
        .post('/api/v1/events')
        .set('Authorization', `Bearer ${unverifiedToken}`)
        .send({
          type: 'pet_help',
          subType: 'lost',
          title: '寻猫启事',
          description: '我家橘猫走丢了',
          petMeta: {
            petType: 'cat',
            breed: '橘猫',
            name: '橘橘',
            lostLocation: '小区南门',
            lostTime: '2026-07-20T10:00:00Z',
            appearance: '橘色短毛，体型偏胖',
            photos: [],
            rewardType: 'negotiable',
            note: '有线索请联系',
          },
        });
      expect(res.status).toBe(201);
      expect(res.body.data.subType).toBe('lost');
    });

    it('未认证用户创建 feed 返回 40301', async () => {
      const res = await request(app.getHttpServer())
        .post('/api/v1/events')
        .set('Authorization', `Bearer ${unverifiedToken}`)
        .send({
          type: 'pet_help',
          subType: 'feed',
          title: '代喂猫咪',
          description: '出差求帮忙',
          petMeta: {
            petType: 'cat',
            feedsPerDay: 2,
            totalDays: 3,
            dateRange: { start: '2026-08-01', end: '2026-08-03' },
            needClean: true,
            rewardType: 'free',
          },
        });
      expect(res.status).toBe(403);
      expect(res.body.code).toBe(40301);
    });

    it('未认证用户创建 walk 返回 40301', async () => {
      const res = await request(app.getHttpServer())
        .post('/api/v1/events')
        .set('Authorization', `Bearer ${unverifiedToken}`)
        .send({
          type: 'pet_help',
          subType: 'walk',
          title: '代遛狗',
          description: '求帮忙',
          petMeta: {
            dogSize: 'small',
            timesPerDay: 1,
            durationPerTime: 20,
            timeSlots: ['morning'],
            needGear: false,
            rewardType: 'free',
          },
        });
      expect(res.status).toBe(403);
      expect(res.body.code).toBe(40301);
    });

    it('type=pet_help 但缺 subType 返回 400', async () => {
      const res = await request(app.getHttpServer())
        .post('/api/v1/events')
        .set('Authorization', `Bearer ${verifiedToken}`)
        .send({
          type: 'pet_help',
          title: '无 subType',
          description: '测试',
        });
      expect(res.status).toBe(400);
    });

    it('feed petMeta 含 photos 字段返回 400', async () => {
      const res = await request(app.getHttpServer())
        .post('/api/v1/events')
        .set('Authorization', `Bearer ${verifiedToken}`)
        .send({
          type: 'pet_help',
          subType: 'feed',
          title: '代喂',
          description: '测试',
          petMeta: {
            petType: 'cat',
            feedsPerDay: 2,
            totalDays: 3,
            dateRange: { start: '2026-08-01', end: '2026-08-03' },
            needClean: false,
            rewardType: 'free',
            photos: ['http://example.com/cat.jpg'],
          },
        });
      expect(res.status).toBe(400);
    });
  });

  describe('列表查询', () => {
    it('filter=pet_help 只返回 pet_help 类型', async () => {
      const res = await request(app.getHttpServer())
        .get('/api/v1/events?type=pet_help')
        .set('Authorization', `Bearer ${verifiedToken}`);
      expect(res.status).toBe(200);
      expect(res.body.data.items.every((e: any) => e.type === 'pet_help')).toBe(true);
    });

    it('多类型 filter 返回 3 种类型', async () => {
      // 先创建一个 help_request 事件
      await request(app.getHttpServer())
        .post('/api/v1/events')
        .set('Authorization', `Bearer ${verifiedToken}`)
        .send({
          type: 'help_request',
          title: '借工具',
          description: '求借电钻',
          rewardType: 'free',
        });

      const res = await request(app.getHttpServer())
        .get('/api/v1/events?type=help_request,public_welfare,pet_help')
        .set('Authorization', `Bearer ${verifiedToken}`);
      expect(res.status).toBe(200);
      const types = new Set(res.body.data.items.map((e: any) => e.type));
      expect(types.has('help_request')).toBe(true);
      expect(types.has('pet_help')).toBe(true);
    });
  });

  describe('老数据迁移验证', () => {
    it('type=lost_found 的事件计数为 0', async () => {
      const count = await prisma.event.count({ where: { type: 'lost_found' } });
      expect(count).toBe(0);
    });
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

```bash
cd apps/api && npx vitest run test/event-pet-help.spec.ts
```

预期失败：所有用例红色（type=pet_help 未支持、subType 字段不存在、未认证用户能创建 feed 等）。

- [ ] **Step 3: 提交（RED 阶段）**

```bash
git add apps/api/test/event-pet-help.spec.ts
git commit -m "test(M22): add failing tests for pet_help event creation + auth + subType validation"
```

---

## Task 4: 后端实现 - CreateEventDto + EventsService 分支

**文件：**

- 修改: `apps/api/src/modules/events/dto/create-event.dto.ts`
- 修改: `apps/api/src/modules/events/events.service.ts` (create 方法, 约 192 行)

- [ ] **Step 1: 扩展 CreateEventDto**

`apps/api/src/modules/events/dto/create-event.dto.ts` 末尾加 2 个字段：

```ts
  @IsOptional()
  @IsEnum(['feed', 'walk', 'lost'])
  subType?: string;

  @IsOptional()
  petMeta?: Record<string, any>;
```

并扩展 `@IsEnum` 装饰器的 type 字段，加 'pet_help'：

```ts
  @IsEnum(['help_request', 'public_welfare', 'pet_help', 'lost_found', 'public_feedback', 'discussion'])
  type: string;
```

- [ ] **Step 2: 扩展 EventsService.create()**

在 `async create(userId, communityId, dto)` 方法中，议事类校验后、AI 审核前，插入 pet_help 分支：

```ts
// M22: pet_help 子分类校验
if (dto.type === 'pet_help') {
  if (!dto.subType) {
    throw new BadRequestException('pet_help 类型必须指定 subType');
  }
  // feed/walk 需要业主认证，lost 不需要
  if (dto.subType === 'feed' || dto.subType === 'walk') {
    const user = await this.prisma.user.findUnique({ where: { id: userId } });
    if (!user || user.verifyStatus !== 'verified') {
      throw new ForbiddenException({
        code: 40301,
        message: '需要业主认证后才能创建代喂/代遛',
      });
    }
  }
  // feed/walk 不允许 photos 字段
  if ((dto.subType === 'feed' || dto.subType === 'walk') && dto.petMeta?.photos) {
    throw new BadRequestException('feed/walk 不支持图片上传');
  }
}
```

> 注意：ForbiddenException 需要 import；用 `code: 40301` 与全局 ErrorCodes 保持一致。

- [ ] **Step 3: 在 create 方法末尾的 prisma.event.create data 中加 2 个字段**

在 `aiComment,` 行后加：

```ts
        subType: dto.type === 'pet_help' ? dto.subType : null,
        petMeta: dto.type === 'pet_help' ? (dto.petMeta as any) : null,
```

- [ ] **Step 4: 运行测试确认通过**

```bash
cd apps/api && npx vitest run test/event-pet-help.spec.ts
```

预期：全部用例绿色。

- [ ] **Step 5: 跑全量测试确保无回归**

```bash
cd apps/api && npm test
```

- [ ] **Step 6: 提交**

```bash
git add apps/api/src/modules/events/dto/create-event.dto.ts apps/api/src/modules/events/events.service.ts
git commit -m "feat(M22): support pet_help event creation with subType + petMeta + auth validation"
```

---

## Task 5: 后端实现 - findAll 支持多类型 filter + subType filter

**文件：**

- 修改: `apps/api/src/modules/events/events.service.ts` (findAll 方法)

- [ ] **Step 1: 检查 findAll 现有 type 处理逻辑**

读 `events.service.ts` findAll 方法，找到 `where.type` 构造处。预期现有逻辑：

```ts
if (type) {
  where.type = type; // 或 type.split(',')
}
```

- [ ] **Step 2: 扩展支持多类型逗号分隔**

修改为支持 `type=help_request,public_welfare,pet_help` 多类型：

```ts
if (type) {
  if (type.includes(',')) {
    where.type = { in: type.split(',') };
  } else {
    where.type = type;
  }
}
if (subType) {
  where.subType = subType;
}
```

- [ ] **Step 3: controller 加 subType query 参数**

`events.controller.ts` 的 `@Get()` findAll 方法参数加：

```ts
    @Query('subType') subType?: string,
```

并把 subType 透传给 service.findAll()。

- [ ] **Step 4: 运行测试确认通过**

```bash
cd apps/api && npx vitest run test/event-pet-help.spec.ts
```

- [ ] **Step 5: 跑全量测试**

```bash
cd apps/api && npm test
```

- [ ] **Step 6: 提交**

```bash
git add apps/api/src/modules/events/events.service.ts apps/api/src/modules/events/events.controller.ts
git commit -m "feat(M22): support multi-type filter and subType filter in events findAll"
```

---

## Task 6: 前端创建页 - 3 套字段配置

**文件：**

- 创建: `apps/miniapp/src/pages/pet-create/index.tsx`
- 创建: `apps/miniapp/src/pages/pet-create/index.scss`
- 创建: `apps/miniapp/src/pages/pet-create/field-configs.ts`
- 修改: `apps/miniapp/src/pages/pet-create/index.config.ts` (Taro 页面配置)

- [ ] **Step 1: 创建 field-configs.ts**

定义 3 套字段配置对象，描述每个字段的 name/label/type/required/options：

```ts
// apps/miniapp/src/pages/pet-create/field-configs.ts
import { PetSubType } from '@xiaoqu-bangbang/shared';

export interface FieldConfig {
  name: string;
  label: string;
  type: 'text' | 'number' | 'textarea' | 'radio' | 'checkbox' | 'switch' | 'date-range' | 'image';
  required: boolean;
  options?: Array<{ label: string; value: string }>;
  placeholder?: string;
}

export const FEED_FIELDS: FieldConfig[] = [
  {
    name: 'petType',
    label: '宠物种类',
    type: 'radio',
    required: true,
    options: [
      { label: '猫', value: 'cat' },
      { label: '狗', value: 'dog' },
      { label: '鱼', value: 'fish' },
      { label: '其他', value: 'other' },
    ],
  },
  { name: 'petName', label: '宠物名字', type: 'text', required: false, placeholder: '可选' },
  { name: 'feedsPerDay', label: '喂食次数/天', type: 'number', required: true },
  { name: 'totalDays', label: '总天数', type: 'number', required: true },
  { name: 'dateRange', label: '起止日期', type: 'date-range', required: true },
  { name: 'needClean', label: '需要清理猫砂/粪便', type: 'switch', required: false },
  {
    name: 'rewardType',
    label: '报酬',
    type: 'radio',
    required: true,
    options: [
      { label: '免费', value: 'free' },
      { label: '面议', value: 'negotiable' },
      { label: '具体金额', value: 'paid' },
    ],
  },
  { name: 'note', label: '备注', type: 'textarea', required: false },
];

export const WALK_FIELDS: FieldConfig[] = [
  {
    name: 'dogSize',
    label: '狗的体型',
    type: 'radio',
    required: true,
    options: [
      { label: '小型', value: 'small' },
      { label: '中型', value: 'medium' },
      { label: '大型', value: 'large' },
    ],
  },
  { name: 'dogName', label: '狗的名字', type: 'text', required: false, placeholder: '可选' },
  { name: 'timesPerDay', label: '每天次数', type: 'number', required: true },
  { name: 'durationPerTime', label: '每次时长（分钟）', type: 'number', required: true },
  {
    name: 'timeSlots',
    label: '时间段要求',
    type: 'checkbox',
    required: true,
    options: [
      { label: '早晨', value: 'morning' },
      { label: '中午', value: 'noon' },
      { label: '傍晚', value: 'evening' },
      { label: '夜间', value: 'night' },
    ],
  },
  { name: 'needGear', label: '需要牵引绳/尿垫', type: 'switch', required: false },
  {
    name: 'rewardType',
    label: '报酬',
    type: 'radio',
    required: true,
    options: [
      { label: '免费', value: 'free' },
      { label: '面议', value: 'negotiable' },
      { label: '具体金额', value: 'paid' },
    ],
  },
  { name: 'note', label: '备注', type: 'textarea', required: false },
];

export const LOST_FIELDS: FieldConfig[] = [
  { name: 'petType', label: '宠物种类', type: 'text', required: true, placeholder: '如：猫/狗' },
  { name: 'breed', label: '品种', type: 'text', required: false, placeholder: '可选' },
  { name: 'name', label: '名字', type: 'text', required: false, placeholder: '可选' },
  { name: 'lostLocation', label: '走丢地点', type: 'text', required: true },
  {
    name: 'lostTime',
    label: '走丢时间',
    type: 'text',
    required: true,
    placeholder: '如：2026-07-20 10:00',
  },
  {
    name: 'appearance',
    label: '外观特征',
    type: 'textarea',
    required: false,
    placeholder: '毛色/体型/特殊标记',
  },
  { name: 'photos', label: '照片', type: 'image', required: false },
  {
    name: 'rewardType',
    label: '酬谢',
    type: 'radio',
    required: true,
    options: [
      { label: '免费', value: 'free' },
      { label: '面议', value: 'negotiable' },
      { label: '具体金额', value: 'paid' },
    ],
  },
  { name: 'note', label: '备注', type: 'textarea', required: false },
];

export function getFields(subType: string): FieldConfig[] {
  switch (subType) {
    case PetSubType.FEED:
      return FEED_FIELDS;
    case PetSubType.WALK:
      return WALK_FIELDS;
    case PetSubType.LOST:
      return LOST_FIELDS;
    default:
      return [];
  }
}
```

- [ ] **Step 2: 创建页面 index.tsx**

```tsx
// apps/miniapp/src/pages/pet-create/index.tsx
import { useState } from 'react';
import Taro from '@tarojs/taro';
import {
  View,
  Text,
  Input,
  Textarea,
  Radio,
  RadioGroup,
  Label,
  Checkbox,
  CheckboxGroup,
  Switch,
  Button,
} from '@tarojs/components';
import { useAuthStore } from '@/store/auth';
import { getFields, FieldConfig } from './field-configs';
import './index.scss';

interface PetCreateProps {}

const PetCreate: React.FC<PetCreateProps> = () => {
  const subType = Taro.getCurrentInstance().router?.params?.type || 'feed';
  const fields = getFields(subType);
  const [form, setForm] = useState<Record<string, any>>({});
  const { verifyStatus } = useAuthStore();

  // 业主认证前置（feed/walk）
  if ((subType === 'feed' || subType === 'walk') && verifyStatus !== 'verified') {
    Taro.showModal({
      title: '需要业主认证',
      content: '代喂/代遛需要业主认证后才能发布，是否前往认证？',
      success: (res) => {
        if (res.confirm) Taro.redirectTo({ url: '/pages/verify/index' });
        else Taro.navigateBack();
      },
    });
    return null;
  }

  const setField = (name: string, value: any) => {
    setForm({ ...form, [name]: value });
  };

  const handleSubmit = async () => {
    // 校验必填
    for (const f of fields) {
      if (f.required && !form[f.name]) {
        Taro.showToast({ title: `请填写${f.label}`, icon: 'none' });
        return;
      }
    }
    // 构造 petMeta
    const petMeta: Record<string, any> = {};
    for (const f of fields) {
      if (form[f.name] !== undefined) petMeta[f.name] = form[f.name];
    }
    const payload = {
      type: 'pet_help',
      subType,
      title:
        subType === 'feed'
          ? `代喂${petMeta.petName || ''}`
          : subType === 'walk'
            ? `代遛${petMeta.dogName || ''}`
            : `寻宠-${petMeta.name || petMeta.petType}`,
      description: petMeta.note || '',
      petMeta,
    };
    // 调用创建 API
    const res = await Taro.request({
      url: `${process.env.TARO_APP_API}/api/v1/events`,
      method: 'POST',
      header: {
        Authorization: `Bearer ${Taro.getStorageSync('token')}`,
        'Content-Type': 'application/json',
      },
      data: payload,
    });
    if (res.data?.code === 0) {
      Taro.showToast({ title: '发布成功', icon: 'success' });
      setTimeout(() => Taro.navigateBack(), 1500);
    } else {
      Taro.showToast({ title: res.data?.message || '发布失败', icon: 'none' });
    }
  };

  const renderField = (f: FieldConfig) => {
    switch (f.type) {
      case 'text':
        return (
          <Input
            value={form[f.name] || ''}
            placeholder={f.placeholder}
            onInput={(e) => setField(f.name, e.detail.value)}
          />
        );
      case 'number':
        return (
          <Input
            type="number"
            value={form[f.name] || ''}
            onInput={(e) => setField(f.name, Number(e.detail.value))}
          />
        );
      case 'textarea':
        return (
          <Textarea
            value={form[f.name] || ''}
            placeholder={f.placeholder}
            onInput={(e) => setField(f.name, e.detail.value)}
          />
        );
      case 'radio':
        return (
          <RadioGroup onChange={(e) => setField(f.name, e.detail.value)}>
            {f.options!.map((o) => (
              <Label key={o.value}>
                <Radio value={o.value} checked={form[f.name] === o.value} />
                {o.label}
              </Label>
            ))}
          </RadioGroup>
        );
      case 'checkbox':
        return (
          <CheckboxGroup onChange={(e) => setField(f.name, e.detail.value)}>
            {f.options!.map((o) => (
              <Label key={o.value}>
                <Checkbox value={o.value} />
                {o.label}
              </Label>
            ))}
          </CheckboxGroup>
        );
      case 'switch':
        return (
          <Switch checked={!!form[f.name]} onChange={(e) => setField(f.name, e.detail.value)} />
        );
      case 'date-range':
        // 简化：用两个 date input
        return (
          <View>
            <Input
              type="text"
              placeholder="开始日期"
              onInput={(e) => setField(f.name, { ...(form[f.name] || {}), start: e.detail.value })}
            />
            <Input
              type="text"
              placeholder="结束日期"
              onInput={(e) => setField(f.name, { ...(form[f.name] || {}), end: e.detail.value })}
            />
          </View>
        );
      case 'image':
        // 复用现有 image-picker 组件
        return null; // TODO: 接入 ImagePicker 组件
      default:
        return null;
    }
  };

  return (
    <View className="pet-create">
      <View className="form">
        {fields.map((f) => (
          <View key={f.name} className="form-item">
            <Text className="label">
              {f.label}
              {f.required && ' *'}
            </Text>
            {renderField(f)}
          </View>
        ))}
      </View>
      <Button onClick={handleSubmit}>发布</Button>
    </View>
  );
};

export default PetCreate;
```

- [ ] **Step 3: 创建 index.scss（基础样式）**

```scss
.pet-create {
  padding: 32rpx;
  .form-item {
    margin-bottom: 24rpx;
    .label {
      display: block;
      font-size: 28rpx;
      color: #333;
      margin-bottom: 8rpx;
    }
  }
  button {
    margin-top: 48rpx;
  }
}
```

- [ ] **Step 4: 创建页面配置 + 注册路由**

```ts
// apps/miniapp/src/pages/pet-create/index.config.ts
export default {
  navigationBarTitleText: '发布宠物帮帮',
};
```

在 `apps/miniapp/src/app.config.ts` 的 `pages` 数组加：

```ts
'pages/pet-create/index',
```

- [ ] **Step 5: 构建验证**

```bash
cd apps/miniapp && npm run build:weapp
```

- [ ] **Step 6: 提交**

```bash
git add apps/miniapp/src/pages/pet-create/ apps/miniapp/src/app.config.ts
git commit -m "feat(M22): add pet-create page with 3 subType field configs"
```

---

## Task 7: 前端编辑页

**文件：**

- 创建: `apps/miniapp/src/pages/pet-edit/index.tsx`
- 创建: `apps/miniapp/src/pages/pet-edit/index.scss`
- 创建: `apps/miniapp/src/pages/pet-edit/index.config.ts`
- 修改: `apps/miniapp/src/app.config.ts`

- [ ] **Step 1: 创建编辑页**

复用 pet-create 的字段配置，预填数据：

```tsx
// apps/miniapp/src/pages/pet-edit/index.tsx
import { useState, useEffect } from 'react';
import Taro from '@tarojs/taro';
import { View, Text, Button } from '@tarojs/components';
import { getFields } from '../pet-create/field-configs';
// 复用 pet-create 的 renderField 逻辑（抽成共享组件更佳，但本期 YAGNI）
import './index.scss';

const PetEdit = () => {
  const eventId = Taro.getCurrentInstance().router?.params?.id;
  const [event, setEvent] = useState<any>(null);
  const [form, setForm] = useState<Record<string, any>>({});

  useEffect(() => {
    Taro.request({
      url: `${process.env.TARO_APP_API}/api/v1/events/${eventId}`,
      method: 'GET',
      header: { Authorization: `Bearer ${Taro.getStorageSync('token')}` },
    }).then((res) => {
      if (res.data?.code === 0) {
        setEvent(res.data.data);
        setForm(res.data.data.petMeta || {});
      }
    });
  }, [eventId]);

  const handleSave = async () => {
    const res = await Taro.request({
      url: `${process.env.TARO_APP_API}/api/v1/events/${eventId}`,
      method: 'PATCH',
      header: {
        Authorization: `Bearer ${Taro.getStorageSync('token')}`,
        'Content-Type': 'application/json',
      },
      data: { petMeta: form },
    });
    if (res.data?.code === 0) {
      Taro.showToast({ title: '保存成功', icon: 'success' });
      setTimeout(() => Taro.navigateBack(), 1500);
    }
  };

  if (!event) return null;
  const fields = getFields(event.subType);

  // 沿用 pet-create 的 renderField（ponytail: 实际抽组件更好，本期先用最小实现）
  return (
    <View className="pet-edit">
      <View className="form">{/* 字段渲染同 pet-create，复用 renderField 逻辑 */}</View>
      <Button onClick={handleSave}>保存</Button>
    </View>
  );
};

export default PetEdit;
```

> ponytail: 实际实现时把 pet-create 的 renderField 抽成 `<PetFormFields fields={fields} form={form} onChange={setField} />` 共享组件，编辑页直接复用。上限：3 个页面共享一份字段渲染逻辑。

- [ ] **Step 2: 创建 scss + config**

样式和 pet-create 一致，config 标题改"编辑宠物帮帮"。

- [ ] **Step 3: 注册路由**

`app.config.ts` pages 数组加 `'pages/pet-edit/index'`。

- [ ] **Step 4: 构建验证**

```bash
cd apps/miniapp && npm run build:weapp
```

- [ ] **Step 5: 提交**

```bash
git add apps/miniapp/src/pages/pet-edit/ apps/miniapp/src/app.config.ts
git commit -m "feat(M22): add pet-edit page reusing pet-create field configs"
```

---

## Task 8: 前端 filter 改造 + 卡片 typeLabel

**文件：**

- 修改: `apps/miniapp/src/pages/events/index.tsx`
- 修改: `apps/miniapp/src/components/masonry-card/index.tsx` (typeLabel 映射)

- [ ] **Step 1: 修改 events 页 filter 数组**

```tsx
// 原 filter 数组（4 项 - 移除"我能帮忙"，加"宠物帮帮"）
const FILTERS = [
  { label: '全部', value: 'all' },
  { label: '求助', value: 'help_request' },
  { label: '公益', value: 'public_welfare' },
  { label: '宠物帮帮', value: 'pet_help' },
  // M23 阶段会加：{ label: '购物拼拼', value: 'group_buy' },
];
```

- [ ] **Step 2: 修改 fetchEvents 根据 filter 调用**

```tsx
const fetchEvents = async (filter: string) => {
  let url = `${API}/api/v1/events?page=${page}&pageSize=${pageSize}`;
  if (filter === 'all') {
    // M22 阶段：3 种类型联合查询（M23 阶段改调 /feed/all 含 group_buy）
    url += '&type=help_request,public_welfare,pet_help';
  } else if (filter === 'pet_help') {
    url += '&type=pet_help';
  } else {
    url += `&type=${filter}`;
  }
  // ... 其余逻辑不变
};
```

- [ ] **Step 3: 修改 FAB 路由**

```tsx
const handleFabClick = () => {
  if (filter === 'pet_help') {
    Taro.navigateTo({ url: '/pages/pet-create/index' });
  } else {
    Taro.navigateTo({ url: '/pages/event-create/index' });
  }
};
```

- [ ] **Step 4: 扩展 MasonryEventCard 的 typeLabel 映射**

在 `masonry-card/index.tsx` 的 typeLabel 映射处加：

```ts
const TYPE_LABELS: Record<string, string> = {
  help_request: '求助',
  public_welfare: '公益',
  'pet_help.feed': '代喂',
  'pet_help.walk': '代遛',
  'pet_help.lost': '寻宠',
  // M23 阶段加：
  // 'group_buy.seek': '求代购',
  // 'group_buy.offer': '代购方',
};

// 使用时：
const typeLabel = event.subType
  ? TYPE_LABELS[`${event.type}.${event.subType}`]
  : TYPE_LABELS[event.type];
```

- [ ] **Step 5: 构建验证**

```bash
cd apps/miniapp && npm run build:weapp
```

- [ ] **Step 6: 提交**

```bash
git add apps/miniapp/src/pages/events/index.tsx apps/miniapp/src/components/masonry-card/
git commit -m "feat(M22): events page filter 4 items + pet-help FAB route + typeLabel mapping"
```

---

## Task 9: 详情页支持 petMeta 字段展示

**文件：**

- 修改: `apps/miniapp/src/pages/event-detail/index.tsx`

- [ ] **Step 1: 在详情页加 petMeta 字段展示区**

在事件描述后、评论区前，加 petMeta 渲染：

```tsx
{
  event.type === 'pet_help' && event.petMeta && (
    <View className="pet-meta">
      <Text className="section-title">详细信息</Text>
      {event.subType === 'feed' && (
        <>
          <Text>宠物种类：{event.petMeta.petType}</Text>
          {event.petMeta.petName && <Text>名字：{event.petMeta.petName}</Text>}
          <Text>喂食次数/天：{event.petMeta.feedsPerDay}</Text>
          <Text>总天数：{event.petMeta.totalDays}</Text>
          <Text>
            起止：{event.petMeta.dateRange?.start} ~ {event.petMeta.dateRange?.end}
          </Text>
          <Text>需要清理：{event.petMeta.needClean ? '是' : '否'}</Text>
        </>
      )}
      {event.subType === 'walk' && (
        <>
          <Text>狗的体型：{event.petMeta.dogSize}</Text>
          {event.petMeta.dogName && <Text>名字：{event.petMeta.dogName}</Text>}
          <Text>每天次数：{event.petMeta.timesPerDay}</Text>
          <Text>每次时长：{event.petMeta.durationPerTime} 分钟</Text>
          <Text>时间段：{event.petMeta.timeSlots?.join('、')}</Text>
          <Text>需要牵引绳：{event.petMeta.needGear ? '是' : '否'}</Text>
        </>
      )}
      {event.subType === 'lost' && (
        <>
          <Text>种类：{event.petMeta.petType}</Text>
          {event.petMeta.breed && <Text>品种：{event.petMeta.breed}</Text>}
          {event.petMeta.name && <Text>名字：{event.petMeta.name}</Text>}
          <Text>走丢地点：{event.petMeta.lostLocation}</Text>
          <Text>走丢时间：{event.petMeta.lostTime}</Text>
          {event.petMeta.appearance && <Text>外观：{event.petMeta.appearance}</Text>}
          {event.petMeta.photos?.length > 0 && (
            <View className="photos">
              {event.petMeta.photos.map((p: string) => (
                <Image key={p} src={p} mode="aspectFill" />
              ))}
            </View>
          )}
        </>
      )}
    </View>
  );
}
```

- [ ] **Step 2: 构建验证**

```bash
cd apps/miniapp && npm run build:weapp
```

- [ ] **Step 3: 提交**

```bash
git add apps/miniapp/src/pages/event-detail/index.tsx
git commit -m "feat(M22): event-detail page displays petMeta fields by subType"
```

---

## Task 10: 批次 1 验证 + /review

- [ ] **Step 1: 跑全量后端测试**

```bash
cd apps/api && npm test
```

预期：原有 260 测试 + 新 M22 测试全绿。

- [ ] **Step 2: 跑前端构建**

```bash
cd apps/miniapp && npm run build:weapp
```

- [ ] **Step 3: 跑 Admin 构建**

```bash
cd apps/admin && npm run build
```

- [ ] **Step 4: 手工验收（对照 Standard.md M22 条目）**

| 条目   | 验证                                                                  |
| ------ | --------------------------------------------------------------------- |
| M22.1  | `packages/shared/src/enums.ts` 含 PET_HELP + PetSubType 三值          |
| M22.2  | `psql ... SELECT count(*) FROM events WHERE type='lost_found'` 返回 0 |
| M22.3  | curl 创建 feed + 未认证创建返回 40301                                 |
| M22.4  | curl `?type=help_request,public_welfare,pet_help` 返回多类型          |
| M22.5  | curl feed petMeta 含 photos 返回 400                                  |
| M22.6  | events 页 filter 4 项截图                                             |
| M22.7  | 列表 typeLabel 显示"代喂/代遛/寻宠"截图                               |
| M22.8  | pet-create 页 3 套表单截图 + 未认证跳转截图                           |
| M22.9  | pet-edit 页编辑截图                                                   |
| M22.10 | event-detail 页 feed/walk/lost 截图                                   |
| M22.11 | 未认证点 FAB 选 feed/walk 跳认证页截图                                |
| M22.12 | Admin event 列表加 pet_help filter                                    |
| M22.13 | 发布事件通知表查询                                                    |
| M22.14 | feed 单帮手 + lost 多帮手流程日志                                     |

- [ ] **Step 5: 跑 /review skill**

```
/review
```

---

# 批次 2：M23 购物拼拼

## Task 11: Prisma schema 加 GroupBuy + GroupBuyItem 表

**文件：**

- 修改: `apps/api/prisma/schema.prisma` (末尾加 2 个 model)
- 创建: `apps/api/prisma/migrations/20260720000002_add_group_buys/migration.sql`

- [ ] **Step 1: schema.prisma 末尾加 2 个 model**

完整 schema 见设计文档 §3.1。关键字段：

- GroupBuy: id/communityId/initiatorId/type/location/departAt/bidCloseAt/quota/serviceFee/deliveryMethod/note/status/aiReviewStatus/aiReviewReason/createdAt/updatedAt/deletedAt
- GroupBuyItem: id/groupBuyId/requesterId/name/qty/note/status/createdAt/updatedAt
- UNIQUE(groupBuyId, requesterId) 防重复响应

需要在 User model 加反向关系：

```prisma
  groupBuysInitiated    GroupBuy[]      @relation("GroupBuyInitiator")
  groupBuyItems         GroupBuyItem[]  @relation("GroupBuyItemRequester")
```

Community model 加：

```prisma
  groupBuys            GroupBuy[]
```

- [ ] **Step 2: 创建 migration SQL**

```sql
CREATE TABLE "group_buys" (
    "id" UUID NOT NULL DEFAULT gen_random_uuid(),
    "community_id" UUID NOT NULL,
    "initiator_id" UUID NOT NULL,
    "type" TEXT NOT NULL,
    "location" TEXT NOT NULL,
    "depart_at" TIMESTAMP(3),
    "bid_close_at" TIMESTAMP(3),
    "quota" INTEGER NOT NULL DEFAULT 5,
    "service_fee" TEXT NOT NULL DEFAULT 'free',
    "delivery_method" TEXT NOT NULL,
    "note" TEXT,
    "status" TEXT NOT NULL DEFAULT 'pending_review',
    "ai_review_status" TEXT NOT NULL DEFAULT 'pending',
    "ai_review_reason" TEXT,
    "created_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updated_at" TIMESTAMP(3) NOT NULL,
    "deleted_at" TIMESTAMP(3),
    CONSTRAINT "group_buys_pkey" PRIMARY KEY ("id")
);

CREATE TABLE "group_buy_items" (
    "id" UUID NOT NULL DEFAULT gen_random_uuid(),
    "group_buy_id" UUID NOT NULL,
    "requester_id" UUID NOT NULL,
    "name" TEXT NOT NULL,
    "qty" INTEGER NOT NULL DEFAULT 1,
    "note" TEXT,
    "status" TEXT NOT NULL DEFAULT 'pending',
    "created_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updated_at" TIMESTAMP(3) NOT NULL,
    CONSTRAINT "group_buy_items_pkey" PRIMARY KEY ("id")
);

CREATE UNIQUE INDEX "group_buy_items_group_buy_id_requester_id_key"
  ON "group_buy_items" ("group_buy_id", "requester_id");

CREATE INDEX "group_buys_community_id_type_status_created_at_idx"
  ON "group_buys" ("community_id", "type", "status", "created_at");

CREATE INDEX "group_buys_initiator_id_idx" ON "group_buys" ("initiator_id");

CREATE INDEX "group_buy_items_group_buy_id_status_idx"
  ON "group_buy_items" ("group_buy_id", "status");

ALTER TABLE "group_buys"
  ADD CONSTRAINT "group_buys_community_id_fkey"
  FOREIGN KEY ("community_id") REFERENCES "communities" ("id") ON DELETE RESTRICT ON UPDATE CASCADE;

ALTER TABLE "group_buys"
  ADD CONSTRAINT "group_buys_initiator_id_fkey"
  FOREIGN KEY ("initiator_id") REFERENCES "users" ("id") ON DELETE RESTRICT ON UPDATE CASCADE;

ALTER TABLE "group_buy_items"
  ADD CONSTRAINT "group_buy_items_group_buy_id_fkey"
  FOREIGN KEY ("group_buy_id") REFERENCES "group_buys" ("id") ON DELETE CASCADE ON UPDATE CASCADE;

ALTER TABLE "group_buy_items"
  ADD CONSTRAINT "group_buy_items_requester_id_fkey"
  FOREIGN KEY ("requester_id") REFERENCES "users" ("id") ON DELETE RESTRICT ON UPDATE CASCADE;
```

- [ ] **Step 3: 执行迁移 + 生成 Client**

```bash
cd apps/api && npx prisma migrate dev --name add_group_buys
pnpm db:generate
```

- [ ] **Step 4: 验证迁移**

```bash
psql -h localhost -p 5433 -U postgres -d gengwang_db -c "\d group_buys"
psql -h localhost -p 5433 -U postgres -d gengwang_db -c "\d group_buy_items"
```

- [ ] **Step 5: 提交**

```bash
git add apps/api/prisma/schema.prisma apps/api/prisma/migrations/20260720000002_add_group_buys/
git commit -m "feat(M23): add GroupBuy + GroupBuyItem tables with migration"
```

---

## Task 12: 共享契约加 GroupBuy 类型

**文件：**

- 修改: `packages/shared/src/enums.ts`
- 修改: `packages/shared/src/api.ts`

- [ ] **Step 1: enums.ts 加 GroupBuy 相关枚举**

```ts
/** 购物拼拼方向 */
export enum GroupBuyType {
  SEEK = 'seek',
  OFFER = 'offer',
}

/** 购物拼拼主表状态 */
export enum GroupBuyStatus {
  PENDING_REVIEW = 'pending_review',
  OPEN = 'open',
  CLOSED_FOR_BID = 'closed_for_bid',
  PURCHASED = 'purchased',
  COMPLETED = 'completed',
  CLOSED = 'closed',
  REJECTED = 'rejected',
}

/** 购物拼拼 item 状态 */
export enum GroupBuyItemStatus {
  PENDING = 'pending',
  CONFIRMED = 'confirmed',
  PURCHASED = 'purchased',
  DELIVERED = 'delivered',
  REJECTED = 'rejected',
}

/** 交付方式 */
export enum GroupBuyDeliveryMethod {
  SELF_PICKUP = 'self_pickup',
  DOOR_DROP = 'door_drop',
  SPOT = 'spot',
}
```

- [ ] **Step 2: api.ts 加 GroupBuy DTO**

```ts
/** 购物拼拼商品项（创建时填） */
export interface GroupBuyItemInput {
  name: string;
  qty?: number;
  note?: string;
}

/** 创建购物拼拼请求 */
export interface CreateGroupBuyRequest {
  type: GroupBuyType;
  location: string;
  departAt?: string; // offer 必填
  bidCloseAt?: string; // offer 必填
  quota?: number; // offer 必填，默认 5
  serviceFee?: string; // free/negotiable/具体金额
  deliveryMethod: GroupBuyDeliveryMethod;
  note?: string;
  items?: GroupBuyItemInput[]; // seek 必填 ≥1
}

/** 购物拼拼商品项 DTO */
export interface GroupBuyItemDto {
  id: string;
  groupBuyId: string;
  requesterId: string;
  requester?: { id: string; nickname: string; avatarUrl: string | null };
  name: string;
  qty: number;
  note?: string | null;
  status: GroupBuyItemStatus;
  createdAt: string;
  updatedAt: string;
}

/** 购物拼拼 DTO */
export interface GroupBuyDto {
  id: string;
  communityId: string;
  initiatorId: string;
  initiator?: { id: string; nickname: string; avatarUrl: string | null };
  type: GroupBuyType;
  location: string;
  departAt: string | null;
  bidCloseAt: string | null;
  quota: number;
  serviceFee: string;
  deliveryMethod: GroupBuyDeliveryMethod;
  note: string | null;
  status: GroupBuyStatus;
  aiReviewStatus: AiReviewStatus;
  aiReviewReason: string | null;
  items: GroupBuyItemDto[];
  _count?: { items: number };
  createdAt: string;
  updatedAt: string;
}

/** 响应购物拼拼 */
export interface RespondGroupBuyRequest {
  name: string;
  qty?: number;
  note?: string;
}

/** Feed 聚合端点统一卡片 DTO */
export interface FeedItemDto {
  id: string;
  sourceType: 'event' | 'group_buy';
  type: string; // EventType 或 GroupBuyType
  subType?: string | null;
  title: string;
  subtitle?: string;
  status: string;
  stats: {
    likeCount?: number;
    commentCount?: number;
    responseCount?: number;
  };
  createdAt: string;
}
```

- [ ] **Step 3: 构建共享包**

```bash
cd packages/shared && pnpm build
```

- [ ] **Step 4: 提交**

```bash
git add packages/shared/src/enums.ts packages/shared/src/api.ts packages/shared/dist/
git commit -m "feat(M23): add GroupBuy enums + DTOs + FeedItemDto"
```

---

## Task 13: 写失败测试 - M23 后端状态机 + 名额 + 自动 completed

**文件：**

- 创建: `apps/api/test/group-buy.spec.ts`

- [ ] **Step 1: 写测试文件**

```ts
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import { Test, TestingModule } from '@nestjs/testing';
import { INestApplication, ValidationPipe } from '@nestjs/common';
import request from 'supertest';
import { AppModule } from '../src/app.module';
import { PrismaService } from '../src/prisma/prisma.service';

describe('Feature: 购物拼拼 (M23)', () => {
  let app: INestApplication;
  let prisma: PrismaService;
  let initiatorToken: string;
  let initiatorId: string;
  let responderToken: string;
  let responderId: string;
  let responder2Token: string;
  let responder2Id: string;
  let communityId: string;
  let seekGroupBuyId: string;
  let offerGroupBuyId: string;

  beforeAll(async () => {
    const moduleFixture: TestingModule = await Test.createTestingModule({
      imports: [AppModule],
    }).compile();
    app = moduleFixture.createNestApplication();
    app.setGlobalPrefix('api/v1');
    app.useGlobalPipes(
      new ValidationPipe({ whitelist: true, forbidNonWhitelisted: true, transform: true }),
    );
    await app.init();
    prisma = app.get(PrismaService);

    const community = await prisma.community.create({
      data: { name: '购物拼拼测试小区', city: '南京', district: '鼓楼区', address: '拼拼路1号' },
    });
    communityId = community.id;

    // 创建 3 个用户并认证
    const users = await Promise.all([
      request(app.getHttpServer()).post('/api/v1/auth/wechat-login').send({ code: 'gb-initiator' }),
      request(app.getHttpServer()).post('/api/v1/auth/wechat-login').send({ code: 'gb-responder' }),
      request(app.getHttpServer())
        .post('/api/v1/auth/wechat-login')
        .send({ code: 'gb-responder2' }),
    ]);
    [initiatorToken, responderToken, responder2Token] = users.map((u) => u.body.data.token);
    [initiatorId, responderId, responder2Id] = users.map((u) => u.body.data.user.id);
    for (const u of users) {
      await prisma.user.update({
        where: { id: u.body.data.user.id },
        data: { verifyStatus: 'verified' },
      });
    }
    for (const t of [initiatorToken, responderToken, responder2Token]) {
      await request(app.getHttpServer())
        .post('/api/v1/communities/select')
        .set('Authorization', `Bearer ${t}`)
        .send({ communityId });
    }
  });

  afterAll(async () => {
    await prisma.groupBuyItem.deleteMany({});
    await prisma.groupBuy.deleteMany({ where: { communityId } });
    await prisma.communityMember.deleteMany({ where: { communityId } });
    await prisma.community.delete({ where: { id: communityId } });
    await prisma.user.deleteMany({
      where: { id: { in: [initiatorId, responderId, responder2Id] } },
    });
    await app.close();
  });

  describe('创建 (M23.2)', () => {
    it('seek 创建成功（items≥1）', async () => {
      const res = await request(app.getHttpServer())
        .post('/api/v1/group-buys')
        .set('Authorization', `Bearer ${initiatorToken}`)
        .send({
          type: 'seek',
          location: '山姆',
          deliveryMethod: 'self_pickup',
          items: [{ name: '咖啡', qty: 2 }],
        });
      expect(res.status).toBe(201);
      seekGroupBuyId = res.body.data.id;
      expect(res.body.data.status).toBe('open'); // AiReview 默认 pass
    });

    it('seek 无 items 返回 400', async () => {
      const res = await request(app.getHttpServer())
        .post('/api/v1/group-buys')
        .set('Authorization', `Bearer ${initiatorToken}`)
        .send({
          type: 'seek',
          location: '山姆',
          deliveryMethod: 'self_pickup',
          items: [],
        });
      expect(res.status).toBe(400);
    });

    it('offer 创建成功（quota+departAt+bidCloseAt）', async () => {
      const res = await request(app.getHttpServer())
        .post('/api/v1/group-buys')
        .set('Authorization', `Bearer ${initiatorToken}`)
        .send({
          type: 'offer',
          location: 'Costco',
          departAt: '2026-08-01T10:00:00Z',
          bidCloseAt: '2026-07-31T20:00:00Z',
          quota: 2,
          deliveryMethod: 'self_pickup',
        });
      expect(res.status).toBe(201);
      offerGroupBuyId = res.body.data.id;
    });

    it('offer 缺 quota 返回 400', async () => {
      const res = await request(app.getHttpServer())
        .post('/api/v1/group-buys')
        .set('Authorization', `Bearer ${initiatorToken}`)
        .send({
          type: 'offer',
          location: 'Costco',
          departAt: '2026-08-01T10:00:00Z',
          bidCloseAt: '2026-07-31T20:00:00Z',
          deliveryMethod: 'self_pickup',
        });
      expect(res.status).toBe(400);
    });
  });

  describe('响应 + 名额 (M23.4)', () => {
    it('响应成功创建 item', async () => {
      const res = await request(app.getHttpServer())
        .post(`/api/v1/group-buys/${offerGroupBuyId}/respond`)
        .set('Authorization', `Bearer ${responderToken}`)
        .send({ name: '牛奶', qty: 1 });
      expect(res.status).toBe(201);
    });

    it('同一用户重复响应失败', async () => {
      const res = await request(app.getHttpServer())
        .post(`/api/v1/group-buys/${offerGroupBuyId}/respond`)
        .set('Authorization', `Bearer ${responderToken}`)
        .send({ name: '面包', qty: 1 });
      expect(res.status).toBe(409);
    });

    it('名额满后响应返回 409', async () => {
      // offer quota=2，已响应 1 人，再 1 人即满
      await request(app.getHttpServer())
        .post(`/api/v1/group-buys/${offerGroupBuyId}/respond`)
        .set('Authorization', `Bearer ${responder2Token}`)
        .send({ name: '鸡蛋', qty: 1 });
      // 第 3 人应该被拒（但需要第 3 个用户，这里简化：用 initiator 响应自己）
      const res = await request(app.getHttpServer())
        .post(`/api/v1/group-buys/${offerGroupBuyId}/respond`)
        .set('Authorization', `Bearer ${initiatorToken}`)
        .send({ name: '水', qty: 1 });
      expect(res.status).toBe(409);
    });
  });

  describe('主买人操作 (M23.3 + M23.5)', () => {
    it('非主买人 confirm 返回 403', async () => {
      // 先取 responder 的 item id
      const detail = await request(app.getHttpServer())
        .get(`/api/v1/group-buys/${offerGroupBuyId}`)
        .set('Authorization', `Bearer ${initiatorToken}`);
      const itemId = detail.body.data.items[0].id;
      const res = await request(app.getHttpServer())
        .post(`/api/v1/group-buys/${offerGroupBuyId}/items/${itemId}/confirm`)
        .set('Authorization', `Bearer ${responderToken}`);
      expect(res.status).toBe(403);
    });

    it('主买人 confirm item', async () => {
      const detail = await request(app.getHttpServer())
        .get(`/api/v1/group-buys/${offerGroupBuyId}`)
        .set('Authorization', `Bearer ${initiatorToken}`);
      const itemId = detail.body.data.items[0].id;
      const res = await request(app.getHttpServer())
        .post(`/api/v1/group-buys/${offerGroupBuyId}/items/${itemId}/confirm`)
        .set('Authorization', `Bearer ${initiatorToken}`);
      expect(res.status).toBe(200);
      expect(res.body.data.status).toBe('confirmed');
    });

    it('主买人 reject item 释放名额', async () => {
      const detail = await request(app.getHttpServer())
        .get(`/api/v1/group-buys/${offerGroupBuyId}`)
        .set('Authorization', `Bearer ${initiatorToken}`);
      const itemId = detail.body.data.items.find((i: any) => i.status === 'pending').id;
      const res = await request(app.getHttpServer())
        .post(`/api/v1/group-buys/${offerGroupBuyId}/items/${itemId}/reject`)
        .set('Authorization', `Bearer ${initiatorToken}`);
      expect(res.status).toBe(200);
    });

    it('close-bid 状态流转', async () => {
      const res = await request(app.getHttpServer())
        .post(`/api/v1/group-buys/${offerGroupBuyId}/close-bid`)
        .set('Authorization', `Bearer ${initiatorToken}`);
      expect(res.status).toBe(200);
      expect(res.body.data.status).toBe('closed_for_bid');
    });

    it('跳过状态返回 400（open 直接 purchased）', async () => {
      // 先创建一个新的 open 状态 offer
      const newOffer = await request(app.getHttpServer())
        .post('/api/v1/group-buys')
        .set('Authorization', `Bearer ${initiatorToken}`)
        .send({
          type: 'offer',
          location: '测试',
          departAt: '2026-08-01T10:00:00Z',
          bidCloseAt: '2026-07-31T20:00:00Z',
          quota: 1,
          deliveryMethod: 'self_pickup',
        });
      const res = await request(app.getHttpServer())
        .post(`/api/v1/group-buys/${newOffer.body.data.id}/purchased`)
        .set('Authorization', `Bearer ${initiatorToken}`);
      expect(res.status).toBe(400);
    });

    it('purchased 状态流转', async () => {
      const res = await request(app.getHttpServer())
        .post(`/api/v1/group-buys/${offerGroupBuyId}/purchased`)
        .set('Authorization', `Bearer ${initiatorToken}`);
      expect(res.status).toBe(200);
      expect(res.body.data.status).toBe('purchased');
    });

    it('deliver 自动 completed', async () => {
      const detail = await request(app.getHttpServer())
        .get(`/api/v1/group-buys/${offerGroupBuyId}`)
        .set('Authorization', `Bearer ${initiatorToken}`);
      // 把所有 confirmed item 都 deliver
      for (const item of detail.body.data.items) {
        if (item.status === 'confirmed') {
          const res = await request(app.getHttpServer())
            .post(`/api/v1/group-buys/${offerGroupBuyId}/items/${item.id}/deliver`)
            .set('Authorization', `Bearer ${initiatorToken}`);
          expect(res.status).toBe(200);
        }
      }
      // 检查主表 status
      const finalDetail = await request(app.getHttpServer())
        .get(`/api/v1/group-buys/${offerGroupBuyId}`)
        .set('Authorization', `Bearer ${initiatorToken}`);
      expect(finalDetail.body.data.status).toBe('completed');
    });
  });

  describe('取消响应 (M23.6)', () => {
    it('截止前取消成功', async () => {
      // 创建新 offer 并响应
      const newOffer = await request(app.getHttpServer())
        .post('/api/v1/group-buys')
        .set('Authorization', `Bearer ${initiatorToken}`)
        .send({
          type: 'offer',
          location: '取消测试',
          departAt: '2026-08-01T10:00:00Z',
          bidCloseAt: '2026-07-31T20:00:00Z',
          quota: 1,
          deliveryMethod: 'self_pickup',
        });
      await request(app.getHttpServer())
        .post(`/api/v1/group-buys/${newOffer.body.data.id}/respond`)
        .set('Authorization', `Bearer ${responderToken}`)
        .send({ name: '测试商品', qty: 1 });
      const res = await request(app.getHttpServer())
        .post(`/api/v1/group-buys/${newOffer.body.data.id}/cancel-response`)
        .set('Authorization', `Bearer ${responderToken}`);
      expect(res.status).toBe(200);
    });
  });

  describe('社区隔离', () => {
    it('跨小区查询返回空', async () => {
      // 创建另一个小区的用户
      const otherCommunity = await prisma.community.create({
        data: { name: '其他小区', city: '上海', district: '浦东', address: '其他路' },
      });
      const otherUser = await request(app.getHttpServer())
        .post('/api/v1/auth/wechat-login')
        .send({ code: 'gb-other' });
      await request(app.getHttpServer())
        .post('/api/v1/communities/select')
        .set('Authorization', `Bearer ${otherUser.body.data.token}`)
        .send({ communityId: otherCommunity.id });

      const res = await request(app.getHttpServer())
        .get('/api/v1/group-buys')
        .set('Authorization', `Bearer ${otherUser.body.data.token}`);
      expect(res.status).toBe(200);
      expect(res.body.data.items.length).toBe(0);

      // 清理
      await prisma.communityMember.deleteMany({ where: { communityId: otherCommunity.id } });
      await prisma.community.delete({ where: { id: otherCommunity.id } });
      await prisma.user.delete({ where: { id: otherUser.body.data.user.id } });
    });
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

```bash
cd apps/api && npx vitest run test/group-buy.spec.ts
```

预期失败：模块不存在、路由 404。

- [ ] **Step 3: 提交（RED 阶段）**

```bash
git add apps/api/test/group-buy.spec.ts
git commit -m "test(M23): add failing tests for group-buy lifecycle + quota + auto-completed"
```

---

## Task 14: 后端实现 - GroupBuy 模块

**文件：**

- 创建: `apps/api/src/modules/group-buys/group-buys.module.ts`
- 创建: `apps/api/src/modules/group-buys/group-buys.controller.ts`
- 创建: `apps/api/src/modules/group-buys/group-buys.service.ts`
- 创建: `apps/api/src/modules/group-buys/dto/create-group-buy.dto.ts`
- 创建: `apps/api/src/modules/group-buys/dto/respond-group-buy.dto.ts`
- 修改: `apps/api/src/app.module.ts`

- [ ] **Step 1: 创建 DTO**

```ts
// apps/api/src/modules/group-buys/dto/create-group-buy.dto.ts
import {
  IsString,
  IsOptional,
  IsEnum,
  IsNumber,
  IsArray,
  ValidateNested,
  Min,
} from 'class-validator';
import { Type } from 'class-transformer';

export class GroupBuyItemInputDto {
  @IsString()
  name: string;

  @IsOptional()
  @IsNumber()
  @Min(1)
  qty?: number;

  @IsOptional()
  @IsString()
  note?: string;
}

export class CreateGroupBuyDto {
  @IsEnum(['seek', 'offer'])
  type: string;

  @IsString()
  location: string;

  @IsOptional()
  departAt?: string;

  @IsOptional()
  bidCloseAt?: string;

  @IsOptional()
  @IsNumber()
  @Min(1)
  quota?: number;

  @IsOptional()
  @IsString()
  serviceFee?: string;

  @IsEnum(['self_pickup', 'door_drop', 'spot'])
  deliveryMethod: string;

  @IsOptional()
  @IsString()
  note?: string;

  @IsOptional()
  @IsArray()
  @ValidateNested({ each: true })
  @Type(() => GroupBuyItemInputDto)
  items?: GroupBuyItemInputDto[];
}

// apps/api/src/modules/group-buys/dto/respond-group-buy.dto.ts
import { IsString, IsOptional, IsNumber, Min } from 'class-validator';

export class RespondGroupBuyDto {
  @IsString()
  name: string;

  @IsOptional()
  @IsNumber()
  @Min(1)
  qty?: number;

  @IsOptional()
  @IsString()
  note?: string;
}
```

- [ ] **Step 2: 创建 Service（关键方法）**

```ts
// apps/api/src/modules/group-buys/group-buys.service.ts
import {
  Injectable,
  BadRequestException,
  ForbiddenException,
  ConflictException,
  NotFoundException,
} from '@nestjs/common';
import { PrismaService } from '../../prisma/prisma.service';
import { AiReviewService } from '../ai-review/ai-review.service';
import { NotificationService } from '../notifications/notifications.service';

@Injectable()
export class GroupBuysService {
  constructor(
    private prisma: PrismaService,
    private aiReview: AiReviewService,
    private notifications: NotificationService,
  ) {}

  async create(userId: string, communityId: string, dto: any) {
    // seek 必填 items≥1
    if (dto.type === 'seek' && (!dto.items || dto.items.length === 0)) {
      throw new BadRequestException('seek 类型必须至少填写 1 个商品');
    }
    // offer 必填 quota+departAt+bidCloseAt
    if (dto.type === 'offer') {
      if (!dto.quota || !dto.departAt || !dto.bidCloseAt) {
        throw new BadRequestException('offer 类型必须填写 quota+departAt+bidCloseAt');
      }
    }

    // AiReview 审核
    const reviewText = `${dto.location} ${dto.note || ''}`;
    const tempId = crypto.randomUUID();
    const aiResult = await this.aiReview.reviewText(reviewText, 'group_buy', tempId, {
      location: dto.location,
    });

    let status: string;
    let aiReviewStatus: string;
    if (aiResult.result === 'pass') {
      status = 'open';
      aiReviewStatus = 'pass';
    } else if (aiResult.result === 'reject') {
      status = 'rejected';
      aiReviewStatus = 'reject';
    } else {
      status = 'pending_review';
      aiReviewStatus = 'manual_review';
    }

    // 创建
    const groupBuy = await this.prisma.groupBuy.create({
      data: {
        communityId,
        initiatorId: userId,
        type: dto.type,
        location: dto.location,
        departAt: dto.departAt ? new Date(dto.departAt) : null,
        bidCloseAt: dto.bidCloseAt ? new Date(dto.bidCloseAt) : null,
        quota: dto.quota ?? (dto.type === 'seek' ? 999 : 5), // seek 不限名额
        serviceFee: dto.serviceFee ?? 'free',
        deliveryMethod: dto.deliveryMethod,
        note: dto.note ?? null,
        status,
        aiReviewStatus,
        // items 只对 seek 创建时入库
        items:
          dto.type === 'seek' && dto.items
            ? {
                create: dto.items.map((it: any) => ({
                  requesterId: userId,
                  name: it.name,
                  qty: it.qty ?? 1,
                  note: it.note ?? null,
                })),
              }
            : undefined,
      },
      include: { items: true },
    });

    return groupBuy;
  }

  async findAll(query: any, communityId: string) {
    const page = Number(query.page) || 1;
    const pageSize = Number(query.pageSize) || 20;
    const where: any = {
      communityId,
      deletedAt: null,
      status: { in: ['open', 'closed_for_bid', 'purchased', 'completed'] },
    };
    if (query.type) where.type = query.type;
    const [items, total] = await Promise.all([
      this.prisma.groupBuy.findMany({
        where,
        orderBy: { createdAt: 'desc' },
        skip: (page - 1) * pageSize,
        take: pageSize,
        include: {
          _count: { select: { items: true } },
          items: { select: { id: true, requesterId: true, status: true } },
        },
      }),
      this.prisma.groupBuy.count({ where }),
    ]);
    return { items, total, page, pageSize };
  }

  async findOne(id: string, communityId: string) {
    const gb = await this.prisma.groupBuy.findFirst({
      where: { id, communityId, deletedAt: null },
      include: {
        items: {
          include: { requester: { select: { id: true, nickname: true, avatarUrl: true } } },
        },
        initiator: { select: { id: true, nickname: true, avatarUrl: true } },
      },
    });
    if (!gb) throw new NotFoundException();
    return gb;
  }

  async update(userId: string, id: string, communityId: string, dto: any) {
    const gb = await this.findOne(id, communityId);
    if (gb.initiatorId !== userId) throw new ForbiddenException('仅发起人可编辑');
    if (!['pending_review', 'open'].includes(gb.status)) {
      throw new BadRequestException('当前状态不可编辑');
    }
    // 编辑触发 AiReview
    const reviewText = `${dto.location ?? gb.location} ${dto.note ?? gb.note ?? ''}`;
    const aiResult = await this.aiReview.reviewText(reviewText, 'group_buy', id, {});
    let status = gb.status;
    if (aiResult.result === 'reject') status = 'rejected';
    else if (aiResult.result === 'manual_review') status = 'pending_review';
    else status = 'open';

    return this.prisma.groupBuy.update({
      where: { id },
      data: {
        location: dto.location ?? gb.location,
        departAt: dto.departAt ? new Date(dto.departAt) : gb.departAt,
        bidCloseAt: dto.bidCloseAt ? new Date(dto.bidCloseAt) : gb.bidCloseAt,
        quota: dto.quota ?? gb.quota,
        serviceFee: dto.serviceFee ?? gb.serviceFee,
        deliveryMethod: dto.deliveryMethod ?? gb.deliveryMethod,
        note: dto.note ?? gb.note,
        status,
        aiReviewStatus: aiResult.result,
      },
    });
  }

  async respond(userId: string, id: string, communityId: string, dto: any) {
    const gb = await this.findOne(id, communityId);
    if (gb.status !== 'open') throw new BadRequestException('当前状态不可响应');

    // 名额校验（事务内 count）
    return this.prisma.$transaction(async (tx) => {
      const responderCount = await tx.groupBuyItem.count({
        where: { groupBuyId: id, status: { not: 'rejected' } },
      });
      // seek 不限名额（quota=999）；offer 按 quota
      if (gb.type === 'offer' && responderCount >= gb.quota) {
        throw new ConflictException({ code: 40900, message: 'QUOTA_EXCEEDED' });
      }
      try {
        return await tx.groupBuyItem.create({
          data: {
            groupBuyId: id,
            requesterId: userId,
            name: dto.name,
            qty: dto.qty ?? 1,
            note: dto.note ?? null,
          },
        });
      } catch (e: any) {
        if (e.code === 'P2002') {
          throw new ConflictException('已响应过，不可重复响应');
        }
        throw e;
      }
    });
  }

  async confirmItem(userId: string, gbId: string, itemId: string, communityId: string) {
    const gb = await this.findOne(gbId, communityId);
    if (gb.initiatorId !== userId) throw new ForbiddenException('仅主买人可操作');
    const item = gb.items.find((i) => i.id === itemId);
    if (!item) throw new NotFoundException('item 不存在');
    if (item.status !== 'pending') throw new BadRequestException('item 状态不可 confirm');
    return this.prisma.groupBuyItem.update({
      where: { id: itemId },
      data: { status: 'confirmed' },
    });
  }

  async rejectItem(userId: string, gbId: string, itemId: string, communityId: string) {
    const gb = await this.findOne(gbId, communityId);
    if (gb.initiatorId !== userId) throw new ForbiddenException('仅主买人可操作');
    const item = gb.items.find((i) => i.id === itemId);
    if (!item) throw new NotFoundException('item 不存在');
    if (item.status !== 'pending') throw new BadRequestException('item 状态不可 reject');
    // reject 后释放名额（UNIQUE 约束不释放，但 count 会减少）
    return this.prisma.groupBuyItem.update({
      where: { id: itemId },
      data: { status: 'rejected' },
    });
  }

  async closeBid(userId: string, id: string, communityId: string) {
    const gb = await this.findOne(id, communityId);
    if (gb.initiatorId !== userId) throw new ForbiddenException('仅主买人可操作');
    if (gb.status !== 'open') throw new BadRequestException('仅 open 状态可截止接单');
    const updated = await this.prisma.groupBuy.update({
      where: { id },
      data: { status: 'closed_for_bid' },
    });
    // 通知所有响应人
    const responders = gb.items.filter((i) => i.status !== 'rejected').map((i) => i.requesterId);
    await Promise.all(
      responders.map((rId) =>
        this.notifications.create(rId, {
          type: 'system',
          title: '拼单已截止接单',
          content: `${gb.location} 的拼单已截止`,
        }),
      ),
    );
    return updated;
  }

  async purchased(userId: string, id: string, communityId: string) {
    const gb = await this.findOne(id, communityId);
    if (gb.initiatorId !== userId) throw new ForbiddenException('仅主买人可操作');
    if (gb.status !== 'closed_for_bid')
      throw new BadRequestException('仅 closed_for_bid 状态可标记已购回');
    const updated = await this.prisma.groupBuy.update({
      where: { id },
      data: { status: 'purchased' },
    });
    const responders = gb.items.filter((i) => i.status !== 'rejected').map((i) => i.requesterId);
    await Promise.all(
      responders.map((rId) =>
        this.notifications.create(rId, {
          type: 'system',
          title: '拼单已购回',
          content: `${gb.location} 的拼单已购回`,
        }),
      ),
    );
    return updated;
  }

  async deliver(userId: string, gbId: string, itemId: string, communityId: string) {
    const gb = await this.findOne(gbId, communityId);
    if (gb.initiatorId !== userId) throw new ForbiddenException('仅主买人可操作');
    const item = gb.items.find((i) => i.id === itemId);
    if (!item) throw new NotFoundException('item 不存在');
    if (!['confirmed', 'purchased'].includes(item.status)) {
      throw new BadRequestException('item 状态不可 deliver');
    }
    // 事务：更新 item + 检查是否全部 delivered + 自动 completed
    return this.prisma.$transaction(async (tx) => {
      const updatedItem = await tx.groupBuyItem.update({
        where: { id: itemId },
        data: { status: 'delivered' },
      });
      const remaining = await tx.groupBuyItem.count({
        where: { groupBuyId: gbId, status: { not: 'delivered' } },
      });
      if (remaining === 0) {
        await tx.groupBuy.update({ where: { id: gbId }, data: { status: 'completed' } });
        // 触发发花：主买人 1 朵（沿用现有 ContributionService）
        // TODO: 注入 ContributionService 调用 awardFlower(initiatorId, 'group_buy', gbId)
      }
      return updatedItem;
    });
  }

  async cancelResponse(userId: string, id: string, communityId: string) {
    const gb = await this.findOne(id, communityId);
    if (gb.status === 'closed_for_bid' || gb.status === 'purchased' || gb.status === 'completed') {
      throw new BadRequestException('截止后不可取消');
    }
    const item = gb.items.find((i) => i.requesterId === userId);
    if (!item) throw new NotFoundException('未找到你的响应');
    if (item.status !== 'pending') throw new BadRequestException('item 已被处理，不可取消');
    return this.prisma.groupBuyItem.delete({ where: { id: item.id } });
  }

  async close(userId: string, id: string, communityId: string) {
    const gb = await this.findOne(id, communityId);
    if (gb.initiatorId !== userId) throw new ForbiddenException('仅主买人可关闭');
    return this.prisma.groupBuy.update({ where: { id }, data: { status: 'closed' } });
  }
}
```

> ponytail: TODO 处发花逻辑 - 注入 ContributionService，沿用现有 event 完成发花模式。上限：完整接入后删除 TODO。

- [ ] **Step 3: 创建 Controller**

```ts
// apps/api/src/modules/group-buys/group-buys.controller.ts
import { Controller, Get, Post, Patch, Body, Param, Query, UseGuards } from '@nestjs/common';
import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';
import { CurrentUser } from '../auth/decorators/current-user.decorator';
import { CurrentCommunityId } from '../communities/decorators/current-community.decorator';
import { GroupBuysService } from './group-buys.service';
import { CreateGroupBuyDto } from './dto/create-group-buy.dto';
import { RespondGroupBuyDto } from './dto/respond-group-buy.dto';

@Controller('group-buys')
@UseGuards(JwtAuthGuard)
export class GroupBuysController {
  constructor(private service: GroupBuysService) {}

  @Post()
  create(@CurrentUser() user, @CurrentCommunityId() cid, @Body() dto: CreateGroupBuyDto) {
    return this.service.create(user.id, cid, dto);
  }

  @Get()
  findAll(@CurrentUser() user, @CurrentCommunityId() cid, @Query() query) {
    return this.service.findAll(query, cid);
  }

  @Get(':id')
  findOne(@CurrentUser() user, @CurrentCommunityId() cid, @Param('id') id: string) {
    return this.service.findOne(id, cid);
  }

  @Patch(':id')
  update(
    @CurrentUser() user,
    @CurrentCommunityId() cid,
    @Param('id') id: string,
    @Body() dto: any,
  ) {
    return this.service.update(user.id, id, cid, dto);
  }

  @Post(':id/respond')
  respond(
    @CurrentUser() user,
    @CurrentCommunityId() cid,
    @Param('id') id: string,
    @Body() dto: RespondGroupBuyDto,
  ) {
    return this.service.respond(user.id, id, cid, dto);
  }

  @Post(':id/items/:itemId/confirm')
  confirmItem(
    @CurrentUser() user,
    @CurrentCommunityId() cid,
    @Param('id') id: string,
    @Param('itemId') itemId: string,
  ) {
    return this.service.confirmItem(user.id, id, itemId, cid);
  }

  @Post(':id/items/:itemId/reject')
  rejectItem(
    @CurrentUser() user,
    @CurrentCommunityId() cid,
    @Param('id') id: string,
    @Param('itemId') itemId: string,
  ) {
    return this.service.rejectItem(user.id, id, itemId, cid);
  }

  @Post(':id/close-bid')
  closeBid(@CurrentUser() user, @CurrentCommunityId() cid, @Param('id') id: string) {
    return this.service.closeBid(user.id, id, cid);
  }

  @Post(':id/purchased')
  purchased(@CurrentUser() user, @CurrentCommunityId() cid, @Param('id') id: string) {
    return this.service.purchased(user.id, id, cid);
  }

  @Post(':id/items/:itemId/deliver')
  deliver(
    @CurrentUser() user,
    @CurrentCommunityId() cid,
    @Param('id') id: string,
    @Param('itemId') itemId: string,
  ) {
    return this.service.deliver(user.id, id, itemId, cid);
  }

  @Post(':id/cancel-response')
  cancelResponse(@CurrentUser() user, @CurrentCommunityId() cid, @Param('id') id: string) {
    return this.service.cancelResponse(user.id, id, cid);
  }

  @Post(':id/close')
  close(@CurrentUser() user, @CurrentCommunityId() cid, @Param('id') id: string) {
    return this.service.close(user.id, id, cid);
  }
}
```

- [ ] **Step 4: 创建 Module**

```ts
// apps/api/src/modules/group-buys/group-buys.module.ts
import { Module } from '@nestjs/common';
import { GroupBuysController } from './group-buys.controller';
import { GroupBuysService } from './group-buys.service';
import { AiReviewModule } from '../ai-review/ai-review.module';
import { NotificationsModule } from '../notifications/notifications.module';

@Module({
  imports: [AiReviewModule, NotificationsModule],
  controllers: [GroupBuysController],
  providers: [GroupBuysService],
  exports: [GroupBuysService],
})
export class GroupBuysModule {}
```

- [ ] **Step 5: app.module.ts 加 GroupBuysModule**

```ts
import { GroupBuysModule } from './modules/group-buys/group-buys.module';
// ...
@Module({
  imports: [
    // ...
    GroupBuysModule,
  ],
})
```

- [ ] **Step 6: 运行测试确认通过**

```bash
cd apps/api && npx vitest run test/group-buy.spec.ts
```

预期：全部用例绿色。

- [ ] **Step 7: 跑全量测试**

```bash
cd apps/api && npm test
```

- [ ] **Step 8: 提交**

```bash
git add apps/api/src/modules/group-buys/ apps/api/src/app.module.ts
git commit -m "feat(M23): add GroupBuys module with 12 endpoints + state machine + auto-completed"
```

---

## Task 15: 后端实现 - /feed/all 聚合端点

**文件：**

- 创建: `apps/api/src/modules/feed/feed.module.ts`
- 创建: `apps/api/src/modules/feed/feed.controller.ts`
- 创建: `apps/api/src/modules/feed/feed.service.ts`
- 修改: `apps/api/src/app.module.ts`

- [ ] **Step 1: 创建 FeedService**

```ts
// apps/api/src/modules/feed/feed.service.ts
import { Injectable } from '@nestjs/common';
import { PrismaService } from '../../prisma/prisma.service';

@Injectable()
export class FeedService {
  constructor(private prisma: PrismaService) {}

  async findAll(page: number, pageSize: number, communityId: string) {
    // 并发查 events + group_buys
    const [events, groupBuys] = await Promise.all([
      this.prisma.event.findMany({
        where: {
          communityId,
          type: { in: ['help_request', 'public_welfare', 'pet_help'] },
          status: { in: ['open', 'in_progress', 'processing', 'completed'] },
          deletedAt: null,
        },
        orderBy: { createdAt: 'desc' },
        take: pageSize * page, // 取前 N 条用于合并
        select: {
          id: true,
          type: true,
          subType: true,
          title: true,
          description: true,
          status: true,
          likeCount: true,
          commentCount: true,
          _count: { select: { applications: true } },
          createdAt: true,
        },
      }),
      this.prisma.groupBuy.findMany({
        where: {
          communityId,
          status: { in: ['open', 'closed_for_bid', 'purchased', 'completed'] },
          deletedAt: null,
        },
        orderBy: { createdAt: 'desc' },
        take: pageSize * page,
        select: {
          id: true,
          type: true,
          location: true,
          departAt: true,
          quota: true,
          status: true,
          items: { select: { id: true, status: true } },
          createdAt: true,
        },
      }),
    ]);

    // 映射成 FeedItemDto
    const eventItems = events.map((e) => ({
      id: e.id,
      sourceType: 'event' as const,
      type: e.type,
      subType: e.subType,
      title: e.title,
      subtitle: e.description.slice(0, 50),
      status: e.status,
      stats: {
        likeCount: e.likeCount,
        commentCount: e.commentCount,
        responseCount: e._count.applications,
      },
      createdAt: e.createdAt,
    }));
    const gbItems = groupBuys.map((g) => ({
      id: g.id,
      sourceType: 'group_buy' as const,
      type: g.type,
      subType: null,
      title:
        g.type === 'offer'
          ? `${g.location} ${g.departAt?.toISOString() ?? ''}`
          : `拼单-${g.location}`,
      subtitle:
        g.type === 'offer'
          ? `剩余 ${g.quota - g.items.filter((i) => i.status !== 'rejected').length}/${g.quota} 名额`
          : `${g.items.length} 件商品`,
      status: g.status,
      stats: { responseCount: g.items.filter((i) => i.status !== 'rejected').length },
      createdAt: g.createdAt,
    }));

    // 合并 + 排序 + 分页
    const merged = [...eventItems, ...gbItems].sort(
      (a, b) => b.createdAt.getTime() - a.createdAt.getTime(),
    );
    const start = (page - 1) * pageSize;
    return {
      items: merged.slice(start, start + pageSize),
      total: merged.length,
      page,
      pageSize,
    };
  }
}
```

> ponytail: 内存合并分页在 page>3 时性能下降。当前小区数据量小（<1000 条），YAGNI。升级路径：后续改用 Postgres UNION ALL 跨表查询。

- [ ] **Step 2: 创建 Controller + Module**

```ts
// apps/api/src/modules/feed/feed.controller.ts
import { Controller, Get, Query, UseGuards } from '@nestjs/common';
import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';
import { CurrentCommunityId } from '../communities/decorators/current-community.decorator';
import { FeedService } from './feed.service';

@Controller('feed')
@UseGuards(JwtAuthGuard)
export class FeedController {
  constructor(private service: FeedService) {}

  @Get('all')
  findAll(
    @CurrentCommunityId() cid: string,
    @Query('page') page = 1,
    @Query('pageSize') pageSize = 20,
  ) {
    return this.service.findAll(Number(page), Number(pageSize), cid);
  }
}

// apps/api/src/modules/feed/feed.module.ts
import { Module } from '@nestjs/common';
import { FeedController } from './feed.controller';
import { FeedService } from './feed.service';

@Module({
  controllers: [FeedController],
  providers: [FeedService],
})
export class FeedModule {}
```

- [ ] **Step 3: app.module.ts 加 FeedModule**

- [ ] **Step 4: 写 feed 聚合测试（可选，最小验证）**

`apps/api/test/feed.spec.ts`：

```ts
it('GET /feed/all 返回合并列表', async () => {
  const res = await request(app.getHttpServer())
    .get('/api/v1/feed/all')
    .set('Authorization', `Bearer ${token}`);
  expect(res.status).toBe(200);
  expect(Array.isArray(res.body.data.items)).toBe(true);
});
```

- [ ] **Step 5: 运行测试 + 提交**

```bash
cd apps/api && npx vitest run test/feed.spec.ts
git add apps/api/src/modules/feed/ apps/api/src/app.module.ts apps/api/test/feed.spec.ts
git commit -m "feat(M23): add /feed/all aggregation endpoint for events + group_buys"
```

---

## Task 16: 前端 group-buy 创建页

**文件：**

- 创建: `apps/miniapp/src/pages/group-buy-create/index.tsx`
- 创建: `apps/miniapp/src/pages/group-buy-create/index.scss`
- 创建: `apps/miniapp/src/pages/group-buy-create/index.config.ts`
- 修改: `apps/miniapp/src/app.config.ts`

- [ ] **Step 1: 创建创建页**

```tsx
// apps/miniapp/src/pages/group-buy-create/index.tsx
import { useState } from 'react';
import Taro from '@tarojs/taro';
import { View, Text, Input, Textarea, Radio, RadioGroup, Label, Button } from '@tarojs/components';

const GroupBuyCreate = () => {
  const type = Taro.getCurrentInstance().router?.params?.type || 'seek';
  const [form, setForm] = useState<any>({
    items: type === 'seek' ? [{ name: '', qty: 1, note: '' }] : [],
  });

  const setField = (name, value) => setForm({ ...form, [name]: value });

  const handleSubmit = async () => {
    if (type === 'seek' && (!form.items || form.items.length === 0)) {
      Taro.showToast({ title: '请至少添加 1 个商品', icon: 'none' });
      return;
    }
    if (type === 'offer') {
      if (!form.quota || !form.departAt || !form.bidCloseAt) {
        Taro.showToast({ title: '请填写 quota/出发时间/截止时间', icon: 'none' });
        return;
      }
    }
    const res = await Taro.request({
      url: `${process.env.TARO_APP_API}/api/v1/group-buys`,
      method: 'POST',
      header: {
        Authorization: `Bearer ${Taro.getStorageSync('token')}`,
        'Content-Type': 'application/json',
      },
      data: { type, ...form },
    });
    if (res.data?.code === 0) {
      Taro.showToast({ title: '发布成功', icon: 'success' });
      setTimeout(() => Taro.navigateBack(), 1500);
    }
  };

  return (
    <View className="gb-create">
      <View className="form">
        <View className="form-item">
          <Text>采购地点 *</Text>
          <RadioGroup onChange={(e) => setField('location', e.detail.value)}>
            <Label>
              <Radio value="山姆" />
              山姆
            </Label>
            <Label>
              <Radio value="Costco" />
              Costco
            </Label>
            <Label>
              <Radio value="其他" />
              其他
            </Label>
          </RadioGroup>
        </View>

        {type === 'seek' && (
          <View className="form-item">
            <Text>商品清单 *</Text>
            {form.items.map((it, idx) => (
              <View key={idx} className="item-row">
                <Input
                  placeholder="商品名"
                  value={it.name}
                  onInput={(e) => {
                    const items = [...form.items];
                    items[idx] = { ...it, name: e.detail.value };
                    setField('items', items);
                  }}
                />
                <Input
                  type="number"
                  placeholder="数量"
                  value={String(it.qty)}
                  onInput={(e) => {
                    const items = [...form.items];
                    items[idx] = { ...it, qty: Number(e.detail.value) };
                    setField('items', items);
                  }}
                />
                <Button
                  size="mini"
                  onClick={() =>
                    setField(
                      'items',
                      form.items.filter((_, i) => i !== idx),
                    )
                  }
                >
                  删除
                </Button>
              </View>
            ))}
            <Button
              size="mini"
              onClick={() => setField('items', [...form.items, { name: '', qty: 1, note: '' }])}
            >
              + 添加商品
            </Button>
          </View>
        )}

        {type === 'offer' && (
          <>
            <View className="form-item">
              <Text>出发时间 *</Text>
              <Input
                placeholder="2026-08-01 10:00"
                onInput={(e) => setField('departAt', e.detail.value)}
              />
            </View>
            <View className="form-item">
              <Text>截止接单时间 *</Text>
              <Input
                placeholder="2026-07-31 20:00"
                onInput={(e) => setField('bidCloseAt', e.detail.value)}
              />
            </View>
            <View className="form-item">
              <Text>名额 *</Text>
              <Input
                type="number"
                placeholder="5"
                onInput={(e) => setField('quota', Number(e.detail.value))}
              />
            </View>
          </>
        )}

        <View className="form-item">
          <Text>交付方式 *</Text>
          <RadioGroup onChange={(e) => setField('deliveryMethod', e.detail.value)}>
            <Label>
              <Radio value="self_pickup" />
              自取
            </Label>
            <Label>
              <Radio value="door_drop" />
              送上门
            </Label>
            <Label>
              <Radio value="spot" />
              集中点
            </Label>
          </RadioGroup>
        </View>

        <View className="form-item">
          <Text>备注</Text>
          <Textarea onInput={(e) => setField('note', e.detail.value)} />
        </View>
      </View>

      <Button onClick={handleSubmit}>发布</Button>
    </View>
  );
};

export default GroupBuyCreate;
```

- [ ] **Step 2: scss + config + 路由注册**（同 pet-create 模式）

- [ ] **Step 3: 构建验证 + 提交**

```bash
cd apps/miniapp && npm run build:weapp
git add apps/miniapp/src/pages/group-buy-create/
git commit -m "feat(M23): add group-buy-create page with seek/offer forms"
```

---

## Task 17: 前端 group-buy 详情页 + 编辑页

**文件：**

- 创建: `apps/miniapp/src/pages/group-buy-detail/index.tsx`
- 创建: `apps/miniapp/src/pages/group-buy-detail/index.scss`
- 创建: `apps/miniapp/src/pages/group-buy-detail/index.config.ts`
- 创建: `apps/miniapp/src/pages/group-buy-edit/index.tsx`
- 创建: `apps/miniapp/src/pages/group-buy-edit/index.config.ts`
- 修改: `apps/miniapp/src/app.config.ts`

- [ ] **Step 1: 创建详情页**

详情页需包含：

- 基本信息展示（type/location/departAt/quota/status）
- 商品列表展示
- "我要加入"响应区（填商品+数量提交）
- 主买人视角：confirm/reject/deliver/close-bid/purchased/close 按钮

参考 Task 16 的 Taro.request 模式，根据 groupBuy.status 和 groupBuy.initiatorId 判断显示哪些按钮。

- [ ] **Step 2: 创建编辑页**

复用创建页字段，预填数据 + PATCH 提交。

- [ ] **Step 3: 构建验证 + 提交**

```bash
cd apps/miniapp && npm run build:weapp
git add apps/miniapp/src/pages/group-buy-detail/ apps/miniapp/src/pages/group-buy-edit/
git commit -m "feat(M23): add group-buy-detail + group-buy-edit pages"
```

---

## Task 18: 前端 filter 第 5 项 + /feed/all 接入

**文件：**

- 修改: `apps/miniapp/src/pages/events/index.tsx`
- 修改: `apps/miniapp/src/components/masonry-card/index.tsx` (加 group_buy typeLabel)

- [ ] **Step 1: filter 加第 5 项**

```tsx
const FILTERS = [
  { label: '全部', value: 'all' },
  { label: '求助', value: 'help_request' },
  { label: '公益', value: 'public_welfare' },
  { label: '宠物帮帮', value: 'pet_help' },
  { label: '购物拼拼', value: 'group_buy' }, // 新增
];
```

- [ ] **Step 2: fetchEvents 适配**

```tsx
const fetchEvents = async (filter: string) => {
  let url;
  if (filter === 'all') {
    url = `${API}/api/v1/feed/all?page=${page}&pageSize=${pageSize}`;
  } else if (filter === 'group_buy') {
    url = `${API}/api/v1/group-buys?page=${page}&pageSize=${pageSize}`;
  } else {
    url = `${API}/api/v1/events?type=${filter}&page=${page}&pageSize=${pageSize}`;
  }
  // ... 统一处理返回结果
};
```

- [ ] **Step 3: FAB 路由**

```tsx
const handleFabClick = () => {
  if (filter === 'pet_help') {
    Taro.navigateTo({ url: '/pages/pet-create/index' });
  } else if (filter === 'group_buy') {
    Taro.navigateTo({ url: '/pages/group-buy-create/index' });
  } else {
    Taro.navigateTo({ url: '/pages/event-create/index' });
  }
};
```

- [ ] **Step 4: masonry-card 加 group_buy typeLabel**

```ts
const TYPE_LABELS: Record<string, string> = {
  // ...
  'group_buy.seek': '求代购',
  'group_buy.offer': '代购方',
};
```

- [ ] **Step 5: 构建验证 + 提交**

```bash
cd apps/miniapp && npm run build:weapp
git add apps/miniapp/src/pages/events/index.tsx apps/miniapp/src/components/masonry-card/
git commit -m "feat(M23): events filter adds 5th 'group_buy' item + /feed/all integration + typeLabel"
```

---

## Task 19: Admin 独立管理页

**文件：**

- 创建: `apps/admin/src/app/group-buys/page.tsx`
- 创建: `apps/admin/src/app/group-buys/[id]/page.tsx`
- 修改: `apps/admin/src/components/Layout.tsx` (菜单加"购物拼拼")

- [ ] **Step 1: 创建列表页**

参考现有 `apps/admin/src/app/events/page.tsx` 模式，加 group-buy 列表：

- 表格列：type/location/quota/departAt/status/createdAt
- filter：type/status
- 操作：查看详情、下架（status -> closed）

- [ ] **Step 2: 创建详情页**

展示 GroupBuy 基本信息 + items 列表。

- [ ] **Step 3: Layout 菜单加项**

```tsx
{ key: '/group-buys', label: '购物拼拼', icon: <ShopOutlined /> },
```

- [ ] **Step 4: 构建验证 + 提交**

```bash
cd apps/admin && npm run build
git add apps/admin/src/app/group-buys/ apps/admin/src/components/Layout.tsx
git commit -m "feat(M23): add Admin group-buys management page (list + detail + takedown)"
```

---

## Task 20: 批次 2 验证 + /review

- [ ] **Step 1: 跑全量后端测试**

```bash
cd apps/api && npm test
```

预期：原有测试 + M22 + M23 测试全绿。

- [ ] **Step 2: 跑前端构建**

```bash
cd apps/miniapp && npm run build:weapp
```

- [ ] **Step 3: 跑 Admin 构建**

```bash
cd apps/admin && npm run build
```

- [ ] **Step 4: 手工验收（对照 Standard.md M23 条目）**

| 条目   | 验证                                                                       |
| ------ | -------------------------------------------------------------------------- |
| M23.1  | `psql \d group_buys` + `\d group_buy_items` 字段齐                         |
| M23.2  | curl seek 创建成功 + items=0 返回 400 + offer 缺 quota 400 + AiReview 日志 |
| M23.3  | 完整状态流转日志 + 自动 completed 验证 + 跳过状态 400                      |
| M23.4  | 响应成功 + 同一用户重复响应 409 + 名额满 409                               |
| M23.5  | confirm/reject + 非主买人 403                                              |
| M23.6  | 截止前取消成功 + 截止后 400                                                |
| M23.7  | 通知表查询（截止+购回推所有响应人）                                        |
| M23.8  | filter="购物拼拼" + "全部"截图 + Network 请求                              |
| M23.9  | 创建/详情/编辑 3 页截图 + 详情含响应区 + 主买人按钮                        |
| M23.10 | Admin 列表 + 详情 + 下架操作 + AiReview 日志                               |

- [ ] **Step 5: 跑 /review skill**

```
/review
```

---

## 自检清单

### 设计文档需求覆盖检查

- [x] M22.1 EventType + PetSubType 枚举 -> Task 2
- [x] M22.2 老数据迁移 -> Task 1
- [x] M22.3 后端创建 API + 认证前置 -> Task 4
- [x] M22.4 后端"全部"filter 联合查询 -> Task 5（M22 阶段 3 类型）+ Task 15（M23 扩展含 group_buy）
- [x] M22.5 创建字段校验 -> Task 4
- [x] M22.6 前端 filter 改造 -> Task 8（4 项）+ Task 18（补第 5 项）
- [x] M22.7 卡片 typeLabel -> Task 8 + Task 18
- [x] M22.8 小程序创建页 -> Task 6
- [x] M22.9 小程序编辑页 -> Task 7
- [x] M22.10 详情页复用 -> Task 9
- [x] M22.11 业主认证前置（前端） -> Task 6
- [x] M22.12 管理端 -> 复用现有 event 管理页（无需独立任务）
- [x] M22.13 通知触发 -> 沿用现有 event（无需独立任务）
- [x] M22.14 帮手机制 -> 沿用现有 event（无需独立任务）
- [x] M23.1 数据模型 -> Task 11
- [x] M23.2 后端创建 + AiReview -> Task 14
- [x] M23.3 状态机 + 自动 completed -> Task 14
- [x] M23.4 响应 + 名额 -> Task 14
- [x] M23.5 主买人 confirm/reject -> Task 14
- [x] M23.6 取消响应 -> Task 14
- [x] M23.7 通知触发 -> Task 14
- [x] M23.8 前端列表 + 卡片 -> Task 18
- [x] M23.9 小程序页面 -> Task 16 + Task 17
- [x] M23.10 管理端 -> Task 19

### 占位符检查

- [x] 所有 TODO 已说明上限和升级路径（ponytail 风格）
- [x] 所有 TBD 已展开为具体代码

### 类型/函数名一致性

- [x] `PetSubType` 枚举在 Task 2 定义，Task 4/6 使用
- [x] `GroupBuyType` / `GroupBuyStatus` / `GroupBuyItemStatus` 在 Task 12 定义，Task 14 使用
- [x] `FeedItemDto` 在 Task 12 定义，Task 15 使用
- [x] `getFields()` 在 Task 6 定义，Task 7 使用
- [x] `TYPE_LABELS` 在 Task 8 定义，Task 18 扩展

### 风险点

- Task 4 的 `ForbiddenException` 错误码 40301 需要与现有 ErrorCodes 对齐
- Task 14 的 `NotificationService.create` 签名需要与现有通知服务对齐（参数可能不同）
- Task 14 的 `ContributionService` 发花逻辑需要先查现有 event 完成发花的实现

---

## 执行选择

计划完成后两种执行方式：

1. **子 agent 驱动**（推荐）- 每个 task 派子 agent 执行+审查
2. **内联执行** - 在当前会话中按批次执行
