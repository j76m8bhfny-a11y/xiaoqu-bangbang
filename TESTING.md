# 傻瓜式测试方案

跟着步骤做，每一步都有"看到什么 = 成功"。遇到不对的地方记下来即可。

---

## 第一步：把服务跑起来

### 1️⃣ 检查数据库

打开**终端**，复制粘贴这一行，回车：

```bash
docker ps | grep gengwang_db
```

**看到一行带 `Up` 的输出** = ✅ 成功，进下一步
**啥都没输出** = 运行 `docker start gengwang_db`，再试一次

---

### 2️⃣ 启动后端

复制粘贴这两行，回车：

```bash
cd "/Users/lifengju/【01】我的开发/小区帮/xiaoqu-bangbang-main"
pnpm dev:api
```

**等 30 秒**，看到一堆绿色的 `[Nest] ...` 日志，最后一行是：

```
🚀 API server running on http://localhost:3000/api/v1
```

= ✅ 成功，**这个终端不要关**

---

### 3️⃣ 启动管理后台

**另外开一个新终端**（重要：不要关上面那个），复制粘贴：

```bash
cd "/Users/lifengju/【01】我的开发/小区帮/xiaoqu-bangbang-main"
pnpm dev:admin
```

**等 1 分钟**，看到：

```
▲ Next.js 14.x.x
- Local:        http://localhost:3001
✓ Ready in xxx ms
```

= ✅ 成功（端口可能是 3001 或 3100，**记住这个端口**）

---

### 4️⃣ 打开浏览器

打开 **Chrome**，地址栏输入：

```
http://localhost:3001
```

（用上一步看到的端口，不一定是 3001）

**看到登录页** = ✅ 成功

**登录**：

- 用户名：`zhangsan_admin`
- 密码：`admin123`

**进到后台首页（左侧有菜单，能看到数字卡片）** = ✅ 成功

---

## 第二步：测试新功能（核心，必测）

### 🆕 测试 1：闲置管理 - 隐藏 / 恢复 / 拒绝

1. 左侧点 **闲置管理**
2. 看到一个表格，里面有几条记录
3. **找一条「状态」是「在售」的**

#### ▶ 测「隐藏」

- 在那一行最右边点 **「隐藏」按钮**
- ✅ 右上角弹出绿色提示「已隐藏」
- ✅ 那一行状态变成「已隐藏」（灰色标签）

#### ▶ 测「恢复」

- 在同一行点 **「恢复」按钮**
- ✅ 弹出「已恢复」
- ✅ 状态变回「在售」（绿色标签）

#### ▶ 测「拒绝」

- 在同一行点 **「拒绝」按钮**
- ✅ 弹出一个对话框，里面有「拒绝原因」输入框
- 输入：`测试拒绝`，点「确定」
- ✅ 弹出「已拒绝」
- ✅ 状态变成「已拒绝」（红色标签）

#### ▶ 收尾

- 再点一次「恢复」让它回到「在售」（不影响后面测试）

**全过 = ✅ 测试 1 通过**

---

### 🆕 测试 2：投票管理 - 编辑

1. 左侧点 **投票管理**
2. **找一条「状态」是「草稿」的投票**（如果没有，先点「新增投票」随便建一条）
3. 在那一行最右边点 **「编辑」按钮**

✅ 弹出对话框，里面预填了**原本的标题、描述、截止时间**

- 把标题改成：`测试编辑-XXX`（XXX 随便填）
- 点「确定」
- ✅ 弹出「已更新」
- ✅ 表格里那一行标题变成了你刚改的内容

#### ▶ 反向验证（重要）

- 找一条「状态」**不是**「草稿」的投票（比如「进行中」「已结束」）
- ✅ 那一行**不应该**有「编辑」按钮

**全过 = ✅ 测试 2 通过**

---

### 🆕 测试 3：奖章管理 - 新增

1. 左侧点 **榜单与奖章**
2. 上方点 **「奖章管理」** Tab
3. 点表格上方的 **「新增奖章」按钮**

✅ 弹出对话框，有 4 个输入框：代码、名称、描述、图标URL

- 代码：`test_xxx`（xxx 随便填字母数字）
- 名称：`测试奖章`
- 描述：`这是测试用的奖章`
- 图标URL：`https://placehold.co/64x64`
- 点「确定」

✅ 弹出「奖章已创建」
✅ 表格里出现新的一行，code 是你刚填的

#### ▶ 反向验证（重要）

- 再点一次「新增奖章」，**code 用刚才一样的**（如 `test_xxx`）
- 点「确定」
- ✅ 应该弹出**红色错误**（提示重复或操作失败）

**全过 = ✅ 测试 3 通过**

---

### 🆕 测试 4：PRD 7 批次新功能

> 以下功能在 PRD 锁版开发 7 批次中实现，需逐项验证

#### ▶ 事件结构化评价（batch 3）

1. 创建一个互助事件 → 走完双方确认完成流程
2. 进入事件详情页 → 底部应出现 **「评价」** 按钮
3. 点击 → 弹出评价表单（星级 + 文字评价）
4. 提交 → ✅ 评价成功，列表显示评价内容

#### ▶ 闲置评价（batch 3）

1. 创建闲置商品 → 标记已售（需选买家）
2. 买卖双方都应能在闲置详情页看到 **「评价」** 入口
3. 提交评价 → ✅ 评价列表显示

#### ▶ 闲置意向系统（batch 5）

1. 用账号 A 发布闲置商品
2. 用账号 B 进入闲置详情 → 点 **「我想要」** → ✅ 意向记录创建
3. 账号 A 在详情页看到意向列表 → 点 **「卖给」** 选买家 → ✅ 标记已售

#### ▶ 多帮手流程（batch 6）

1. 创建一个 `public_welfare`（公益）类型事件，设 capacity > 1
2. 多人报名 → 创建者可 **多选** 参与者
3. 逐个确认 → ✅ 每确认一个发一朵花

#### ▶ 个人技能档案（batch 7）

1. 「我的」→ 技能管理 → 添加一条技能（如"能借网线钳"）
2. 创建一个 `help_request` 事件
3. 事件详情页应显示 **匹配的技能列表**（昵称、技能、联系方式）

#### ▶ 事件自动过期（batch 4）

1. 查看 API 日志或手动触发 EventsCron
2. 确认 open/in_progress/processing 超 30 天的事件被自动关闭

#### ▶ 帮手选择 UI（batch 2）

1. 创建事件 → 有人响应 → 进入事件详情
2. 创建者应看到 **「选择帮手」** 按钮
3. CTA 按钮按事件状态正确显示/隐藏

**全过 = ✅ 测试 4 通过**

---

## 第三步：测试老功能没被改坏（快速过）

左侧菜单逐个点一遍，每个**只看「列表能加载出来」**：

| 点击       | 看到                   |
| ---------- | ---------------------- |
| 首页/看板  | 4 个数字卡片有数据     |
| 事件管理   | 表格有内容（事件列表） |
| 用户管理   | 表格有内容（用户列表） |
| 业委会管理 | 表格有内容             |
| 通知管理   | 表格有内容             |
| 服务提供方 | 表格有内容             |
| 横幅管理   | 表格有内容             |
| 反馈管理   | 能加载                 |
| 系统设置   | 能加载                 |

**全部都能加载（即使表格是空的也算过）= ✅ 通过**

---

## 第四步：小程序测试（可选，需要微信开发者工具）

### 5️⃣ 启动小程序

**再开一个新终端**：

```bash
cd "/Users/lifengju/【01】我的开发/小区帮/xiaoqu-bangbang-main"
pnpm dev:miniapp
```

等到看到 `编译完成` 或 `Compiled successfully`。

### 6️⃣ 用微信开发者工具打开

1. 打开**微信开发者工具**
2. 新建/导入项目
3. 目录选：`/Users/lifengju/【01】我的开发/小区帮/xiaoqu-bangbang-main/apps/miniapp/dist`
4. AppID 选「测试号」
5. 点「确定」打开

### 7️⃣ 测试 mine 页

进入后，点底部 **「我的」** tab：

#### ▶ 测「我的服务」（之前是开发中提示）

- 滚动到中间，点 **「我的服务」** 一行
- ✅ 应该**跳转到一个新页面**（服务提供方列表）
- ✅ 不再弹「功能开发中」
- 点左上角返回回到「我的」

#### ▶ 测「邀请邻居」（之前是开发中提示）

- 点 **「邀请邻居」** 一行
- ✅ 弹出对话框，内容是：「点击右上角"···"，选择"转发"，把小区帮榜棒分享给邻居吧～」
- 点「知道了」关闭

#### ▶ 测分享卡片

- 点右上角胶囊菜单 **「···」**
- 点 **「转发」**
- ✅ 弹出转发卡片，标题类似「XX小区的邻居都在用「小区帮榜棒」」或「邻里互助，从小区帮榜棒开始」

**全过 = ✅ 小程序测试通过**

---

## 第五步：测完关掉服务

回到每个终端，按 **Ctrl + C** 停止。

---

## 📋 总验收表

复制下面这个表到记事本，测完打 ✅ 或 ❌：

```
[ ] 环境启动：数据库 + API + Admin 都能起
[ ] 登录后台成功

[ ] 测试 1：闲置「隐藏」按钮工作正常
[ ] 测试 1：闲置「恢复」按钮工作正常
[ ] 测试 1：闲置「拒绝」按钮带原因输入，工作正常

[ ] 测试 2：草稿投票能编辑，预填了原值
[ ] 测试 2：非草稿投票没有编辑按钮

[ ] 测试 3：能新增奖章
[ ] 测试 3：重复 code 会失败

[ ] 测试 4：事件结构化评价表单可提交
[ ] 测试 4：闲置评价列表显示
[ ] 测试 4：闲置意向"我想要"可创建
[ ] 测试 4：markSold 能选买家
[ ] 测试 4：多帮手事件可多选参与者
[ ] 测试 4：逐个确认发花正常
[ ] 测试 4：技能档案可添加
[ ] 测试 4：事件详情显示匹配技能
[ ] 测试 4：CTA 按钮按状态正确显示

[ ] 测试 4：所有老菜单都能正常加载

[ ] 测试 5（可选）：小程序「我的服务」跳转正常
[ ] 测试 5（可选）：小程序「邀请邻居」弹引导对话框
[ ] 测试 5（可选）：小程序分享卡片正常
```

---

## ❓ 遇到问题怎么办

| 现象                                        | 处理                                                    |
| ------------------------------------------- | ------------------------------------------------------- |
| API 起不来，报 `database connection failed` | `docker start gengwang_db` 后再试                       |
| API 起不来，报端口被占用                    | 终端运行 `lsof -ti :3000 \| xargs kill -9`              |
| Admin 起不来，报端口被占用                  | 终端运行 `lsof -ti :3001 \| xargs kill -9`              |
| 登录提示密码错误                            | 数据库 seed 没跑，运行 `cd apps/api && pnpm db:seed`    |
| 后台页面白屏                                | 看浏览器控制台（F12），把错误信息复制下来               |
| 任何按钮点了没反应                          | 看浏览器控制台（F12）的 Network tab，看是不是请求报错了 |

把任何**不符合预期**的截图/错误信息发给我，我来排查。
